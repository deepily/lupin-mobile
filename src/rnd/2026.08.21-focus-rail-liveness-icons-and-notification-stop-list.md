# Focus rail liveness + persona icons, notification stop-list, progress-group collapse — plan

**Author**: Tiffany 💍 (session `e082edd7`) · **Date**: 2026-08-21 · **Status**: Rick GO 2026-08-21 · **Phases A + B + C IMPLEMENTED + GREEN** (uncommitted — Rick drives commits)
**Supersedes/extends**: `2026.06.25-focus-ui-active-history-filter.md` (the rail window part is that plan, now ratified)
**Rick's rulings (voice, 2026-08-21)**: rail = **Live (<1h) default + 24h history toggle** · rail avatar = **persona icon, fallback = persona NAME initial** (never the repo id) · stop-list = **one checkbox = hide from tree AND mute TTS** · build order = **A rail → B stop-list → C collapse**

---

## 0. The ask, restated

Lupin's browser client is enormous; mobile cannot show everything the same way. Three concrete pains on the phone:

1. **Focus rail is a mile long and every badge is the same letter.** The rail lists *every sender ever* and renders a sender-id initial.
2. **Tool-call chatter** (`Done: mcp…`, `Done: Bash`, `Done: ToolSearch`) lands as one bubble per call (the browser collapses them into one DOM node) and gets spoken.
3. No user-editable way to say "never show / never speak lines like *this*".

## 1. Measured facts (dev `:7999`, test account, 2026-08-21)

| Fact | Evidence | Consequence |
|---|---|---|
| Cold start fetches **all** senders | `focus_chat_bloc.dart:158` `_repo.senders( email )` with `hours` omitted → **146 senders**; `?hours=24` → **6**; `?hours=1` → **2** | the mile-long rail is a missing bound, server already supports it |
| `/senders` carries **no persona**; `/senders-visible` carries `voice_persona` + `manager_persona` (stamped from the session bridge for live CC sessions, `notifications.py:3083`) | probe | cold start must use `sendersVisible(hours:…)` to seed badges |
| `personasBySender` is filled **only** by live `FocusPersonaUpdated` events (`focus_chat_bloc.dart:253-260`) | code | backfilled senders have no badge until they speak → fallback |
| Fallback renders `sid.substring(0,1)` (`session_rail.dart:55`) | code | the wall of identical letters |
| `Done: <tool>` lines come from `lupin/src/lupin_cli/claude_code/hooks/post_tool_use.py:131-133` **with `progress_group_id`** | code | the browser's "one node" collapse key; mobile parses it (`notification_models.dart:75`) but **nothing consumes it** |
| TTS gate = `tts_orchestrator.dart:154` (`masterMute`) + `:240` (per-priority); focus pane uses `enqueueAlways` (`focus_chat_bloc.dart:93`) | code | stop-list must sit **before** both enqueue sites |
| Prefs pattern = `SharedPreferences` getters/setters (`notification_preferences.dart`) + a settings screen (`notification_audio_settings_screen.dart`) | code | reuse for the stop-list |

## 2. Workstream A — Focus rail: Live/24h toggle + persona icons

**A1 — window + visibility (= the 06-25 plan, unchanged contract)**: keep every card; a derived `visible` flag. `FocusFilter { live, history }`, `lastActivityBySender`, `asOf` + 30s aging tick; **Live = `<1h`, History = `<24h`** (constants mirror the web clients verbatim); `SegmentedButton` with counts in a slim toolbar above rail+pane; rail status dots 🟢/🟡; empty state. Exit handling per 06-25 §4.8 (debounced `voice_persona_released` → hide in Live). Open decision #8 resolved by today's ruling: History = single 24h view.

**A2 — cold start (NEW)**: `_coldStartBuild` → `_repo.sendersVisible( email, hours: 24 )` and **seed `personasBySender` from `SenderSummary.voicePersona`** (add `voicePersona`/`managerPersona` parse to `SenderSummary`). Live `FocusPersonaUpdated` still overrides.

**A3 — avatar fallback (NEW)**: `_badgeFor` fallback initial = `persona?.displayName ?? persona?.name` first letter; if no persona at all, the sender-id **local part before `@`/`#`** first letter (e.g. `deep.research…` → "D"), never the repo. Exited persona (bridge gone) keeps its last icon while the card is visible (A1 retains cards).

**Tests**: bloc (cold-start uses `sendersVisible(hours:24)` + seeds personas; filter selectors; aging tick; exit debounce), widget (toggle, dots, fallback initials, empty state). 100% on new/changed files.

## 3. Workstream B — Notification stop-list (hide + mute)

**Model**: `NotificationStopList` (`lib/services/notification_filter/`) — ordered list of `StopPattern { String pattern; bool enabled; }`, persisted as JSON in `SharedPreferences` key `notif_filter.stop_list`. **Seeds** (enabled): `Done: mcp`, `Done: Bash`, `Done: ToolSearch`, `Done: Read`, `Done: Edit`, `Done: Write`, `Done: Grep`, `Done: Glob`. Match = **case-insensitive prefix** on `message` (trimmed); `matches( NotificationItem ) → bool`.

**Application (three seams, one predicate)**:
- Focus bloc ingest: matched item → stored with `hidden=true` (card retained, not rendered, not counted unread) **and not enqueued to TTS** (`enqueueAlways` skipped).
- Inbox/conversation list: hidden items filtered from the rendered list; "show N hidden" reveal chip.
- `TtsOrchestrator.enqueue*`: predicate checked first (belt + suspenders for any other dispatcher).

**UI**: `NotificationFilterSettingsScreen` — checklist (checked = hide + mute), swipe-to-delete, "+ add pattern" field, "reset to defaults". Linked from Settings next to the audio screen.

**Tests**: unit (matcher, persistence round-trip, seeds), bloc (hidden not rendered/not spoken/not unread), widget (checklist toggles persist). 100%.

## 4. Workstream C — Collapse by `progressGroupId` (default on)

Consecutive items in a sender window sharing `progressGroupId` render as **one expandable row**: latest message as the summary line, `×N` count badge, tap to expand. Pure render grouping in the focus pane + conversation screen; no state deletion, no TTS change (stop-list owns TTS). Settings toggle `notif_filter.collapse_groups` (default true).

**Tests**: widget (N grouped items → 1 row + count; expand shows all; toggle off → N rows). 100%.

## 5. Phasing + ACs

| Phase | Deliverable | AC |
|---|---|---|
| **A** | rail toggle + cold-start bound + persona seeding + name-initial fallback | rail shows only <1h senders by default; History reveals ≤24h; badges show persona icons for live sessions; no sender-id initials; suite green |
| **B** | stop-list model + 3 seams + settings screen | checked patterns neither render nor speak anywhere; unchecked restore; survives restart |
| **C** | progress-group collapse | 10 `Done:` lines in one group → 1 row ×10 |

Each phase: plan-approved → implement → `flutter analyze` clean → unit/bloc/widget 100% on new files → full suite 0 ❌ outside `legacy_quarantine/` → Rick device check (human-only: feel of the toggle, icon legibility).

## 5a. Phase A — implementation record (2026-08-21, Tiffany 💍)

| File | Change |
|---|---|
| `lib/features/notifications/data/notification_models.dart` | `SenderSummary` + `voicePersona` / `managerPersona` (parsed from `senders-visible`) |
| `lib/features/focus_mode/domain/focus_chat_state.dart` | `FocusFilter {live,history}`, `FocusBand`, `liveWindow`=1h / `historyWindow`=24h (web parity), `lastActivityBySender`, `exitedSenders`, `filter`, `asOf`; pure `bandFor` / `isVisible` / `visibleOrder` / `liveCount` / `historyCount`; focused sender always visible |
| `lib/features/focus_mode/domain/focus_chat_event.dart` | `FocusFilterChanged`, `FocusActivityTick`, `FocusSenderExited` |
| `lib/features/focus_mode/domain/focus_chat_bloc.dart` | cold start + reconnect → `sendersVisible(hours: 24)` seeding activity + personas + `asOf`; inbound bumps activity / un-exits; `voice_persona_released` arms a 4s exit debounce (re-assign or inbound cancels); `session_reaped` → immediate exit; injectable `now`, optional periodic `tickInterval` (DI = 30s, tests none); `close()` cancels timers |
| `lib/features/focus_mode/presentation/session_rail.dart` | iterates `visibleOrder`; `railInitial()` = persona display name → name → sender local part (never repo id); 🟢/🟡 status dot; empty-Live hint with "24h (N)" jump |
| `lib/features/focus_mode/presentation/focus_filter_bar.dart` | NEW — `SegmentedButton<FocusFilter>` with live counts |
| `lib/features/focus_mode/presentation/focus_mode_screen.dart` | bar inserted between paused banner and rail+pane Row |
| `lib/app.dart` | `session_reaped` → `FocusSenderExited` |
| `lib/core/di/service_locator.dart` | `tickInterval: 30s` |
| `lib/core/testing/test_keys.dart` | 7 new keys |

**Tests**: bloc +14 (lens group) + state selectors ×2 · widget +5 (bar counts/tap, dots incl. exited=amber, name-initial fallback, empty hint + button, no-hint-when-cold) · wiring +1 (`session_reaped`) · existing stubs `senders`→`sendersVisible`. Focus suites **58/58 ✅**; full `flutter test test/` **509 ✅ / 1 skip / 44 ❌ all `legacy_quarantine/`, 0 outside**; `flutter analyze` 0 new issues on touched files.

**Human-only check (Rick, device)**: feel of the toggle; icon legibility at 28px; whether the 30s aging feels right.

## 5b. Phase B — implementation record (2026-08-21, Tiffany 💍)

| File | Change |
|---|---|
| `lib/services/notification_filter/notification_stop_list.dart` | NEW — `StopPattern` + `NotificationStopList` (`ChangeNotifier`, JSON in `SharedPreferences` key `notif_filter.stop_list`, seeds `Done: mcp/Bash/ToolSearch/Read/Edit/Write/Grep/Glob`, `matches()` = case-insensitive prefix on trimmed message, `setEnabled/add/removeAt/resetToDefaults`, corrupt-pref fallback) |
| `lib/services/tts/tts_orchestrator.dart` | optional `stopList`; matched message muted on `enqueueIfSpeakable` AND the ungated `enqueueAlways` |
| `lib/features/focus_mode/domain/focus_chat_{state,bloc}.dart` | `hiddenCountBySender`; suppressed inbound still establishes/bumps its sender but is not stored / not unread / not spoken; backfill + reconnect fetches filtered by the same predicate; `user_initiated_message` never suppressed |
| `lib/features/focus_mode/presentation/focus_chat_pane.dart` | "N hidden by your stop-list" caption |
| `lib/features/notifications/presentation/conversation_screen.dart` | optional `stopList` (locator fallback); list lens + "N hidden" `ActionChip` reveal toggle; live re-render on list change |
| `lib/features/settings/presentation/notification_filter_settings_screen.dart` | NEW — checklist (checked = hidden + muted), add field, swipe-delete, reset menu |
| `notification_audio_settings_screen.dart` · `focus_mode_screen.dart` drawer | entry points to the stop-list screen |
| `lib/core/di/service_locator.dart` | `NotificationStopList` singleton injected into `TtsOrchestrator` + `FocusChatBloc` |
| `lib/core/testing/test_keys.dart` | 9 keys |

**Tests**: unit stop-list ×7 · orchestrator +2 · focus bloc +4 · conversation screen +1 · pane caption +1 · settings screen ×6 (new file). Full `flutter test test/` **530 ✅ / 1 skip / 44 ❌ all `legacy_quarantine/`, 0 outside**; analyze 0 new issues on touched files. One self-inflicted hang during the pass (a test helper that recursed into itself — caught by the analyzer's unused-symbol warning, fixed).

## 5c. Phase C — implementation record (2026-08-21, Tiffany 💍)

| File | Change |
|---|---|
| `lib/services/notification_filter/progress_group_collapse.dart` | NEW — pure `collapseByProgressGroup( items, keyOf, enabled )` → `MessageGroup` runs of consecutive same-id items (a re-appearing id starts a new group; order preserved) |
| `lib/services/notification_filter/notification_stop_list.dart` | `collapseGroups` pref (`notif_filter.collapse_groups`, default ON) + `setCollapseGroups` |
| `lib/features/focus_mode/presentation/focus_chat_pane.dart` | now Stateful; groups the window → `_CollapsedGroup` (latest bubble + `×N` chip, tap expands in place); pending asks never buried; live re-render on pref change |
| `lib/features/notifications/presentation/conversation_screen.dart` | same over the visible list → `_CollapsedCards` |
| `notification_filter_settings_screen.dart` | "Collapse tool-call bursts" switch |
| `test_keys.dart` | 5 keys |

**Tests**: unit grouping ×3 · stop-list pref +1 · settings toggle +1 · NEW `focus_chat_pane_collapse_test.dart` ×2 (×3 row, expand, ask stays separate, pref off/on live) · conversation +1. **Final full `flutter test test/` (A+B+C): 538 ✅ / 1 skip / 44 ❌ all `legacy_quarantine/`, 0 outside** (baseline before this work: 488). `flutter analyze` 0 new issues on every touched file.

## 6. Open decisions (non-blocking)

1. Stop-list match on `title` too, or `message` only? *(rec: message only; title rarely carries the noise)*
2. Should hidden items still bump the **History** count? *(rec: no — hidden is hidden everywhere)*
3. Collapse also in the Inbox list (not just focus pane)? *(rec: yes, same widget)*
