# 2026-04-21 — Stage 4 Agentic Coverage + Tier 2 Polish (Auto-Pilot Session)

## Context

Prior session (`1fb8dc65`, 2026-04-19 → 2026-04-20) closed out the hot-bug list
(URL-encoding + WS lifecycle wiring + HTTP interceptor idempotency) and validated
end-to-end on emulator. **178/178 unit + widget tests green.** User is away for
lunch; wants me to run the queue on auto-pilot, executing only hands-free items
(no Android build / adb / on-device / keyboard-required work).

This plan re-orders the post-session TODO queue so everything executable on the
dev server runs first and device/laptop-bound work is deferred to user's return.

## Goals

1. Close out Testing Playbook Stage 4 — fixture-backed tests for the 9 agentic
   job submission endpoints + widget-test coverage for the 8 forms that don't
   yet have them. This is the largest remaining untested surface in the v0.1.6
   resync.
2. Ship Tier 2 polish items that are pure Flutter UI work (date-grouped
   ConversationScreen, generate-gist button, TrustStateScreen drilldown).
3. Close stale/incorrect TODOs discovered during the scan.

## Hands-Free Execution Sequence

### Phase 0 — Plan serialization *(this file)*

- Serialize the plan to `src/rnd/v0.1.7/` per documentation-first protocol
- Track progress via TaskCreate

### Phase 1 — Stale TODO cleanup + baseline test run

**Discovery made during plan-mode exploration:**

- The `getIt` import in `lib/features/home/home_screen.dart` is **NOT present**
  (confirmed via `Grep -n getIt lib/`). It was removed in a prior session.
  → Drop the corresponding TODO line.
- `lib/shared/models/notification_item.dart` is **NOT orphan**. It's re-exported
  via `lib/shared/models/models.dart` which is imported by 20+ production files
  (voice, audio, cache, repositories). There are now two `NotificationItem`
  classes (the old one in `shared/models/` and a newer, differently-shaped one
  in `lib/features/notifications/data/notification_models.dart`). Deleting the
  old one would require migrating all the voice/audio/cache callers and is
  out-of-scope for this session.
  → Revise the TODO to record that finding rather than "decide whether to
  remove".

Baseline: `./flutter.sh test` → confirm the 178 previous tests still green.

### Phase 2 — Agentic fixture-capture infrastructure

- Extend `src/scripts/_fixture_lib.py` with agentic-specific helpers if needed
  (the existing primitives cover most of what's required).
- New `src/scripts/capture-agentic-fixtures.py` modeled on
  `capture-notifications-fixtures.py`:
  - Login (re-uses `lib.login_for_capture`)
  - Capture submission-response shapes for each of the 9 agentic job types
    (dry-run payloads where supported to avoid spawning real work)
  - Capture one representative artifact-list + job-status response
  - Redact timestamps + user IDs + email + any generated job IDs
  - Safety-net `assert_no_jwt_residue` after writing

### Phase 3 — Capture fixtures against `:7999`

- Env vars already set: `LUPIN_TEST_INTERACTIVE_MOCK_JOBS_EMAIL` / `_PASSWORD`.
- Server confirmed up (HTTP 200 on `/docs`).
- Run `python src/scripts/capture-agentic-fixtures.py`; verify output
  `test/fixtures/agentic/*.json`.

### Phase 4 — Convert `agentic_repository_test.dart` to fixture-backed

- Rewrite to load fixtures via `test/_helpers/fixture_loader.dart` instead of
  hand-stubbed JSON.
- Mirror the pattern established in `test/unit/notifications/notification_repository_test.dart`.
- Ensure deliberate fixture mutation triggers test failure (drift detection).

### Phase 5 — TestKeys + widget tests for 8 remaining agentic forms

Target forms (already screen-tested for DR):

1. `podcast_generator_form.dart`
2. `presentation_generator_form.dart`
3. `swe_team_form.dart`
4. `bug_fix_expediter_form.dart`
5. `test_fix_expediter_form.dart`
6. `test_suite_form.dart`
7. `research_to_podcast_form.dart`
8. `research_to_presentation_form.dart`

For each:
- Apply `TestKeys` / `bySemanticsIdentifier` to submission button + key inputs
- One widget test file mirroring `deep_research_form_test.dart`:
  - Renders without error
  - Validates required fields
  - Dry-run submit dispatches correct BLoC event

### Phase 6 — Tier 2 polish: date-grouped `ConversationScreen`

- Backend method `getConversationByDate(senderId, userEmail)` already wrapped in
  `NotificationRepository.conversationByDate()`. Fixture exists.
- Adapt UI: collapse flat message list into `SliverGroupedListView`-style
  rendering keyed by date; existing per-message cards unchanged.
- Widget test asserts headers + messages render correctly for multi-date input.

### Phase 7 — Tier 2 polish: generate-gist UI + TrustStateScreen drilldown

**generate-gist:**
- Add "Summarize" AppBar action on `ConversationScreen`.
- Dispatches `NotificationBloc.generateGist(senderId, userEmail)` and shows
  result in a bottom sheet.
- Widget test: tap triggers event + renders result.

**TrustStateScreen:**
- New screen launched from `TrustDashboardScreen` per-decision card.
- Shows domain-level trust context (recent decisions, trust settings).
- Uses existing `DecisionProxyRepository.trust()` method.
- Widget test: renders populated state + empty-state case.

### Phase 8 — Final verify + doc updates

- `./flutter.sh test` → expect ≥ 220 unit + widget passing
- Update `TODO.md`:
  - Remove getIt item (stale)
  - Update `notification_item.dart` item with finding + classification
  - Mark Stage 4 + Tier 2 polish items done
- Update `history.md` with session accomplishments
- `bug-fix-queue.md` unchanged (no new bugs, no fixes)

## Verification Strategy

- Full flutter test suite must stay green end-to-end.
- Spot-check one captured fixture by mutating a field and re-running the
  matching test — must fail loudly (drift detection sanity check).
- Widget tests run on plain Flutter test harness (no device).

## Out of Scope (save for user's return)

- On-device sanity pass (5 items in TODO.md) — needs laptop + adb
- Cross-repo parent-Lupin bug fix (`NotificationFifoQueue._emit_queue_update`) —
  different repo
- Stats dashboard + `fl_chart` integration
- In-app audio playback wiring in `AudioArtifactPlayer` (needs server-side
  field-name confirmation)
- Patrol 4.x / Maestro MCP / Alchemist goldens (all deferred w/ revisit triggers)

## Files Expected to Change

```
src/scripts/_fixture_lib.py                     (maybe — agentic helpers)
src/scripts/capture-agentic-fixtures.py         (new)
test/fixtures/agentic/*.json                    (new — ~9-12 files)
test/unit/agentic/agentic_repository_test.dart  (convert to fixture-backed)
lib/core/testing/test_keys.dart                 (add agentic form keys)
lib/features/agentic/presentation/*_form.dart   (TestKeys only — 8 files)
test/widget/agentic/*_form_test.dart            (new — 8 files)
lib/features/notifications/presentation/conversation_screen.dart  (date grouping + gist action)
lib/features/notifications/domain/notification_{event,state,bloc}.dart  (if generateGist not yet wired)
lib/features/decision_proxy/presentation/trust_state_screen.dart  (new)
lib/features/decision_proxy/presentation/trust_dashboard_screen.dart  (drilldown wiring)
test/widget/notifications/conversation_screen_test.dart  (date grouping + gist)
test/widget/decision_proxy/trust_state_screen_test.dart  (new)
TODO.md, history.md                             (session-end updates)
```

## Notes / Constraints

- No curl (per project CLAUDE.md); use `urllib` everywhere.
- `./flutter.sh test` is the canonical test runner.
- Memory directive: don't recommend on-device manual testing while widget-test
  coverage exists as an alternative.
