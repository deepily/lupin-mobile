# Quick Ask — push-to-talk Q&A screen (round 1)

## Context

Rick wants the simplest useful version of the notification client's Q&A card, on the phone: hold a
big pulsing button, speak a question, release, and get an answer back — with an honest status
indicator in between. The questions are **short-lived agentic runs**, not the long-running deep
research / podcast / SWE jobs.

This is deliberately **round 1 of two**. Round 2 (a later session) becomes a multi-job window that
tracks concurrent requests as CJ-Flow-shaped cards across **todo / running / done / dead**, mirroring
the notifications-client accordion. Round 1 is one live question at a time — but every data shape
here is chosen so round 2 is a rendering exercise, not a rewrite.

**Round 1 has two halves.** The Quick Ask screen itself (§1–§6), and — per Rick's ruling 5 — the
start of the **generic channel for the server asking the phone a question** (§6a), because that
channel can block the server outright and is the strategically important half.

### What already exists (reuse, don't rebuild)

| Need | Already built |
|---|---|
| Push-to-talk capture | `lib/services/asr/asr_service.dart` — `startRecording()` / `stopAndTranscribe()` / `cancelRecording()`, WAV 44.1k mono → `/api/upload-and-transcribe-wav` → transcript. Mic permission handled, with a `requestMicPermission` test seam on `voice_reply_field.dart` |
| Asking | `QueueRepository.ask()` → `POST /api/v2/ask` |
| Speech + pause/resume/replay | `lib/services/tts/tts_orchestrator.dart` — `pause()` holds at an utterance boundary (nothing dropped), `resume()`, and an `urgent` enqueue already preempts and **replays the interrupted utterance from the start** |
| WS transport | `lib/services/websocket/websocket_service.dart`; mobile subscribes with `subscribed_events: []`, which the server reads as `["*"]` — **all frames already arrive** |

### The gap this plan closes

The server emits **`job_state_transition`** per job — `pending→queued` (`todo_fifo_queue.py:1400`),
`queued→running` (`queue_consumer.py:198`), `running→completed` (`running_fifo_queue.py:636`; `:741` is running→**STALLED**, a different transition — do not cite it here) —
and the **completed frame carries the answer** in `metadata.response_text`. Status and answer ride
the same wire.

**Mobile drops every one of these frames**: `WsBlocDispatcher` (`lib/app.dart:43`) has no case for
`job_state_transition`. It handles `queue_todo_update` / `queue_running_update` / `queue_done_update`
/ `queue_dead_update` instead — and those four names have **zero emit sites in the server** and are
absent from `websocket available events` in the INI. Nothing has ever emitted them; the parent repo
already measured this and wrote the receipt into `src/tests/lupin_smoke/test_queue_workflow.py:280`.

⇒ Mobile's job-status wiring is dead today. `QueueDashboardScreen` has never live-updated. Wiring
`job_state_transition` is both what Quick Ask needs and the repair for that.

---

## Findings that shape the design (all verified in-tree)

**1. The status frame beats the HTTP response — always, not sometimes.**
`v2/executor.py:198-201` calls `self.todo_queue.push( job )` — which emits `pending→queued`
*synchronously inside `push()`* — and only then `return Outcome( status="waiting", job_id=... )`.
The frame is on the wire before FastAPI serializes the body. On a cache hit the consumer can also
emit `queued→running` and `running→completed` in that same window. **A client that starts listening
after it learns the job id will routinely miss frames, and can miss the entire job.** ⇒ buffer
unattributable frames.

**2. There is no terminal event for a server restart.** `JobState.INTERRUPTED` appears only in a DB
startup sweep (`job_persistence.py:683,728`), never in a WS emit. A bounce mid-job produces *no*
frame — the UI spins forever. ⇒ a silence watchdog is required, not optional.

**3. `GET /api/job-history/{job_id}` is the wrong reconcile door — it will 404.** All PostgreSQL
persistence is gated on `is_agentic_job_type()` (`queue_util.py:91`) against a 10-entry allowlist
(`job_persistence.py:77`). A plain question is not an agentic type, so no row is written. The
working door is **`QueueRepository.getQueue('done'|'dead'|'run'|'todo')`** — `routers/queues.py:391`
puts `response_text` into the done-queue metadata and `JobSummary` already parses it.

**4. Two gates silently swallow the answer.** The queue speaks as `queue.done@lupin.deepily.ai` with
no persona, so `TtsSender.isPersona` is false and `_systemSenderMuted` drops it when
`speakSystemSenders` is off; and `_formatSpeech` cuts every message to `prefs.ttsFraction` —
**Rick set that to 20% on 08-21**. Both were built for previewing chatter the user did not ask for.
⇒ see Rick's ruling, §Decisions.

**5. `AskResponse` has no `waiting` branch — a live bug.** `queue_models.dart:219-221`: `isDone` /
`needsInput` / `isFailed` are all false for `status:'waiting'`, so `summary` falls through to
`'Done ($path)'`. `submit_job_sheet.dart` pops its sheet and reports success for work that has not
started. `pushAgentic()` already knows about `'waiting'`; the knowledge never reached the model.

**6. `WebSocketService.sessionId` is not stable** — refetched on every `_establishConnection()`,
so it is null before connect and changes across reconnects. Harmless for correlation (transitions
are user-scoped) but it cannot be a correlation key.

**7. The completed metadata is already card-shaped.** `running_fifo_queue.py:1692-1710` carries
`question_text, agent_type, status, is_cache_hit, started_at, completed_at, duration_seconds`
(+ `abstract`/`report_link`/`cost_summary` on agentic paths), tagged in-source *"Phase 6.2:
Card-rendering fields for client-side card creation."* It is field-compatible with `JobSummary`.
**Parse all of it in round 1 and round 2's card view is mostly built.**

---

## Decisions (Rick's rulings)

| # | Ruling |
|---|---|
| 1 | **Send immediately on release** — no confirm step. Transcript still renders as the question bubble. |
| 2 | **Auto-speak**, with **tap-to-pause** (resume later) and **replay from the beginning**. |
| 3 | **Scrollback kept**, **record button pinned at top**, **newest Q&A pair on top**. |
| 4 | **Answers always full, always audible** — override both the preview-fraction slider and the mute-system-senders switch. *"We are requesting an ask-and-answer pair, audio in audio out… what we really want is to pause and/or rewind an answer we've already captured."* Those two settings keep governing everything else. |
| 5 | **Start the generic "server asks the phone a question" surface now** — overriding my recommended shortcut. *"I'm more interested in allowing the server to respond with whatever it needs to satisfy a given query… how can the server talk to us on the cell phone when it needs information from us — that is a strategically important piece of behavior."* Noting it can be a **synchronous blocking call: all processing halts on the server until the phone answers.** |

---

## Design

### Phase 0 — serialize, then cascade review (Rick, 2026-08-29)

🔴 **This document is for PLANNING ONLY. Rick's approval covers serialization and nothing else.**

1. Serialize to `src/rnd/2026.08.29-quick-ask-push-to-talk-screen.md` + README link.
2. Run it through the **cascaded review process, with María as workflow steward**.
3. **No implementation begins without Rick's explicit go-ahead after that review.** The open
   decision in §6a.4 is one of the things the cascade must resolve.

### 1. Shared lifecycle vocabulary — `lib/features/queue/domain/job_lifecycle.dart` (NEW)

Mirror the server's own map (`src/cosa/rest/job_state.py:74`) rather than inventing one. Note
**`stalled → todo`, not dead** — a stalled job is resumable; a hand-rolled map gets this wrong.

```dart
enum JobLane { todo, run, done, dead }

enum JobLifecycleState {
  pending, queued, scheduled, paused, running,
  completed, failed, interrupted, cancelled, stalled;

  static JobLifecycleState? parse( String? raw );  // null on unknown ⇒ drop the frame
  JobLane get lane;                                // verbatim STATE_TO_UI_CONTAINER
  int     get rank;                                // monotonic guard, see §3
  bool    get isTerminal;                          // completed|failed|cancelled|interrupted
}
```

Quick Ask's three displayed forms are a **pure projection** of `lane` — no fourth concept is
introduced, so round 2's grouping is `groupBy( entries, (e) => e.state.lane )` with zero rework.
It lives in the *existing* queue feature's domain layer so both screens import it without either
depending on the other.

### 2. Card model — `lib/features/quick_ask/data/quick_ask_models.dart` (NEW)

`QuickAskEntry` carrying the **full** finding-7 metadata, with two constructors —
`fromTransition( Map data )` and `fromSummary( JobSummary s )` — so a frame and a reconcile fetch
produce the same shape.

### 3. `QuickAskBloc` — `lib/features/quick_ask/domain/` (NEW)

A dedicated bloc, **not** an extension of `QueueBloc`: `QueueBloc`'s state is a single-slot
discriminated union whose members clobber each other, and `_onExternalUpdate` early-returns unless
the dashboard is loaded — an in-flight ask would be wiped by any dashboard refresh.

Correlation is a **rank-monotonic reducer with a pre-attribution buffer and a silence watchdog**:

- **Buffer — FILTER ON INSERT, not only on drain.** Before the job id is known, candidate frames land
  in a bounded ring (32 entries / 60s TTL). On the `AskResponse`, drain filtered by `jobId` and fold
  in rank order. *Answers finding 1.*

  > 🔴 **Why insert-time filtering is load-bearing** (Pocholo, 2026-08-29). A drain-time-only filter
  > loses frames silently: the ring evicts on insert, so **a burst of foreign frames can evict your
  > own before the `jobId` filter is ever applied**. You then fold an incomplete history and never
  > know it happened.
  >
  > **Sizing the cap cannot fix this, for a structural reason.** Transitions go out via
  > `emit_to_user_and_admins_sync` (`websocket_manager.py:682`) = the owner **plus every admin,
  > excluding the owner**. On an admin account the fan-out is **every other user's jobs** —
  > unbounded, and not a number any cap can be sized against.
  >
  > **But "filter by `jobId` on insert" is impossible by construction** — the buffer exists precisely
  > because the `jobId` is not known yet. The filter must key on something known at submit time.
  > Measured, per frame:
  >
  > | Frame | carries `session_id` | carries `question_text` |
  > |---|---|---|
  > | `pending → queued` (`todo_fifo_queue.py:1388`) | ✅ | ✅ |
  > | `queued → running` (`queue_consumer.py:191`) | ❌ | ✅ |
  > | `running → completed` (`running_fifo_queue.py:1691`) | ❌ | ✅ |
  >
  > ⇒ **Two keys are on all three frames** — `question_text` (the transcript we just sent) and
  > **`user_email`** (verified present in all three metadata blocks). **Insert rule:** keep a frame
  > only while a submission is live AND `metadata.user_email` is ours AND
  > (`metadata.question_text` equals the submitted transcript **OR** `metadata.session_id` equals our
  > WebSocket session id). Everything else is dropped at the door.
  >
  > **Why `user_email`, and why text alone was not enough** — Chloé asked whether `ffa98fa` *closed*
  > the cap or merely *narrowed* it. Fair: with the admin fan-out, another user asking the
  > **identical question** produces frames that pass a text-only filter. The email key eliminates that
  > case outright. The only residual is **one user with two identical questions in flight**, which
  > round 1's one-live-question guard makes impossible — so for round 1 the cap is **closed, not
  > narrowed**. Round 2 lifts that guard and must re-open the question.
  >
  > Round 1's one-live-question guard makes the text key sufficient: one outstanding question, and a
  > window bounded by the ask returning (~1s normally). **Round 2 lifts that guard and must revisit
  > this** — two identical questions in flight would share a text key. Recorded so the card-view round
  > does not inherit it silently.
  >
  > After filtering, our own job contributes **at most three frames**, so the cap stops being the
  > control and becomes slack. Keep it as a memory bound, not as the correctness mechanism.
- **Monotonic fold** — apply a frame only if `rank(to) > rank(current)`, except terminals which
  always apply. Duplicates and reorders become no-ops; a missed `running` is repaired by `completed`
  landing directly.
- **Watchdog** — one timer, reset on every accepted frame **and on any `response_requested`
  notification** (AC-S1.4b). First probe at **45s** of silence, then 90s, 180s.

  > **Where 45s comes from** — it was unsourced (Chloé). It is a **probe cadence, not a verdict
  > deadline**; my draft conflated the two. A probe is one cheap queue listing whose worst outcome is
  > a single strike and whose best outcome resets the ladder, so probing early is nearly free. It sits
  > below the server's 120s stall threshold **deliberately** — we want to notice a silent job sooner
  > than the server does. What is floored at 120s is the terminal `lost`, not the looking. A
  > conversational question normally completes in a few seconds, so 45s is about an order of magnitude
  > of headroom over the expected case: long enough not to chatter, short enough to catch a dead
  > socket inside a minute.
- **Reconcile** — `getQueue('done')` → `('dead')` → `('run')`/`('todo')` scanning for the job id.
  **Never `getJobHistoryEntry` — finding 3.** *Answers finding 2.* Four outcomes, and **found-alive is
  NOT a step toward `lost`**:

  | Reconcile result | Action |
  |---|---|
  | in `done` | complete with `responseText` — terminal |
  | in `dead` | failed with `error` — terminal |
  | **in `run` or `todo` — the server can SEE it** | **RESET the ladder to its first interval and clear the strike count.** Positive proof of life |
  | **not found anywhere** | decrement toward `lost`: strike +1, back off, rearm |

  > 🔴 **F-Chloé-S1-8 — a bug this plan would have SHIPPED, not a gap in it.** My draft ran
  > *found-alive* and *found-nowhere* down **one shared rearm path**, both decrementing. So a job the
  > server can plainly see RUNNING would be declared **`lost` at ~315s** (45+90+180) and the user told
  > it was gone — while the server was happily running it. **Those are opposite signals and cannot
  > share a branch.**
  >
  > **Precedence when reconcile and the folded state disagree** (also unstated before): a **terminal
  > reconcile result wins over a non-terminal folded state** — the listing is the server's own record,
  > and we are only reconciling because frames went missing. A **non-terminal reconcile result never
  > overrides a terminal folded state** — a `completed` frame we actually received is not undone by a
  > listing that has not caught up. Terminal-vs-terminal cannot arise: the first terminal cancels the
  > watchdog.

- **`lost` is floored by the SERVER's own patience, not by a number I picked.** `lost` requires
  consecutive *found-nowhere* results spanning **more than `cj flow consumer stall threshold seconds`**
  (`lupin-app.ini:1061`, currently **120s**) — the interval after which the server itself gives up and
  emits `RUNNING → STALLED` (`running_fifo_queue.py:1373`). **Never tell the user a job is lost while
  the server still considers it healthy.**
- **Belt channel** — a `notification_queue_update` whose `notification.jobId` matches the live job is
  independent completion evidence (its `message` *is* the answer). One extra dispatcher line.

Also fix finding 5 while here: give `AskResponse` an `isWaiting` branch and stop `summary` reporting
`'Done'` for queued work.

### 4. Screen — `lib/features/quick_ask/presentation/quick_ask_screen.dart` (NEW)

Record button **pinned as a fixed header** (not list item 0, so the list can become the grouped card
view untouched), newest-first `ListView` below. Long-press to record with a pulsing animation,
release to submit. Status chip per card driven by `JobLane`. Pause toggle renders from
`TtsOrchestrator.pausedStream` — **never a local bool**, or this screen and `FocusModeScreen`'s
existing pause toggle will disagree. Replay button on any done card with a non-empty answer.

Registered as a `ListTile` in `_legacyDrawer` (`focus_mode_screen.dart:178`) — the app has no route
table; navigation is imperative pushes.

### 5. Wiring

- `app_constants.dart` — add `eventJobStateTransition = 'job_state_transition'`.
- `lib/app.dart` `WsBlocDispatcher.dispatch` — one new case routing to `QuickAskBloc`, plus one line
  in the existing notification case for the belt channel.
- `service_locator.dart` `_initializeBLoCs()` — register `QuickAskBloc` beside `FocusChatBloc`.
- `app.dart` `MultiBlocProvider` — add the provider. **Load-bearing, not boilerplate**: it forces
  construction at app start so the pre-attribution buffer exists before the first frame.
- `test_keys.dart` — a `quickAsk*` block.

### 6. Speech ownership

`FocusChatBloc` stays the **sole dispatcher of inbound WS frames to speech** (AC-S2.8, pinned by
`test/service_integration/focus_app_wiring_test.dart:83`). Quick Ask does **not** enqueue on a
`completed` frame — the queue already speaks the answer unconditionally, so doing so would double-speak.

Quick Ask drives the orchestrator for **user gestures only**: `pause()` / `resume()`, and replay via
an `urgent` `enqueueAlways` with a named `TtsSender` — urgent preempts and replays from the start
(the existing mechanism, matching ruling 2), and a non-empty `name` makes `isPersona` true so replay
works even when system senders are muted. Amend AC-S2.8's wording from "sole caller" to "sole
frame-driven dispatcher", and add a test that a frame arriving while Quick Ask is mounted still
produces exactly one enqueue.

**Ruling 4 — answers speak in full, always.** Add an optional `verbatim` flag to the orchestrator's
enqueue that skips `_formatSpeech`'s fraction cut and the `_systemSenderMuted` gate.

> 🔴 **`enqueueAlways` has THREE gates, and ruling 4 named two** (S3 review; verified at HEAD). Read
> in order, the third fires **first**:
>
> | # | Gate | What it is | Does `verbatim` bypass it? |
> |---|---|---|---|
> | 1 | `_stopList?.matches( message )` | **the one I omitted** — an explicit user "never speak this" pattern | **NO — see OSQ3** |
> | 2 | `_systemSenderMuted( sender )` | a *class* preference: persona-less senders muted | YES (ruling 4) |
> | 3 | `_formatSpeech`'s `ttsFraction` cut | a *preview* preference | YES (ruling 4) |
>
> Gates 2 and 3 are **preferences about chatter you did not ask for** — ruling 4 overrides them
> correctly. Gate 1 is **a specific ruling the user typed**, which is a different kind of thing, and
> silently overriding it would be worse than the bug.
>
> **But a silently dropped answer is indistinguishable from a hang** — you asked out loud, and
> nothing ever speaks. So: **do not bypass the stop-list; make the suppression VISIBLE — for
> ANSWERS AND QUESTIONS ALIKE.** The surface states "not spoken — matches a stop-list rule", shows
> the rule, and offers a one-tap speak-anyway; the text is always rendered regardless. For a
> **question**, that notice renders in the **prompt widget** with its answer affordance intact
> (AC-S4.14), not in an answer card. *(This paragraph was leftover answer-only copy — the ACs always
> governed and were right; Chloé checked the doc rather than take a summary.)*

> 🔴 **Replay is a NO-OP while paused — and pause-then-rewind is the natural gesture** (S3 review;
> confirmed two ways at HEAD). `enqueueAlways`'s urgent branch preempts only when `!_paused`
> (`tts_orchestrator.dart:266`); while paused it falls through to `_insertBehindLeadingUrgents` and
> then `if ( _current == null ) _tryStartNext()`. `pause()` (`:165`) sets `_paused` **without
> clearing `_current`**, so `_current` is non-null and `_tryStartNext()` is never called — and even
> when it is, `_tryStartNext()` carries its own pause gate. **Silent on both paths.**
>
> Ruling 2 is *"tap to pause… resume when I am able, or perhaps even rewind it to the beginning."*
> **The order Rick describes is pause first, then rewind** — which is exactly the sequence that does
> nothing today. ⇒ **Replay implies resume**: the replay action calls `resume()` before enqueuing, so
> it plays from the start. See AC-S3.4b.

> 🔴 **The same gate pair, reached from the other side — and it is worse** (Pocholo/Clayton,
> 2026-08-29). **Ruling 4 makes ANSWERS audible. Nothing makes QUESTIONS audible.**
>
> Every question the flow asks is sent from `sender_id="ask.flow@lupin.deepily.ai"` with **no
> persona** — verified at both speak paths, `rest/v2/flow.py:939` (`_user_confirms`, Door C) and
> `rest/v2/flow.py:1372` (`_speak`, which carries the Door A interview questions at its `:742` and
> `:1274` call sites). So
> `TtsSender.isPersona` is false and `_systemSenderMuted` drops them whenever `speakSystemSenders` is
> off, with `_formatSpeech` truncating whatever survives.
>
> **Put that beside Door C's blocking behavior and the failure shape is complete:** the phone is
> silently asked a question it never hears · the user, hands-free, has no idea anything was asked ·
> the server sits blocked for up to ~210s · the confirm then defaults to **"no"** · and the cache
> replay that the confirm existed to authorize never happens. Nothing anywhere reports a fault.
>
> ⇒ **The rule generalizes rather than growing a second special case.** Audibility keys on *"the user
> is expected to act on this"*, which covers exactly two things: **an answer they deliberately asked
> for** (ruling 4), and **a question the system is waiting on** (`response_requested`) — the latter
> arguably more strongly, since something is blocked until they reply. See AC-S3.6.
>
> This is a **client-side** fix and stays inside mobile's scope. Stamping a persona on the `ask.flow`
> sender would fix it server-side for every client at once, and is worth raising with the parent
> repo — but it is **not** this plan's to make, and the client should not depend on it happening. `FocusChatBloc`
sets it in **exactly two places, and both are specified** (Chloé — the flag had two setters and only
one was written down):
1. **Answers** — the inbound notification's `job_id` matches a live Quick Ask job; `QuickAskBloc`
   exposes a small read-only `isQuickAskJob( id )`, keeping the enqueue in one place.
2. **Questions** — the inbound notification has `response_requested == true` (AC-S3.6). This setter
   is **not** Quick-Ask-scoped and fires for the whole focus surface.

🔴 **So "nothing else changes behavior" is FALSE and has been removed** (Chloé). Setter 2 makes
**every existing focus-mode question speak in full**, which is the intended repair — but AC-S3.3
cannot catch it, because AC-S3.3 only pins the **flag-off** path. Treated the same way as
AC-S4.10b: **S3's behavior change on the existing surface is a CLOSED LIST of one** — every
`response_requested` notification now speaks verbatim, everywhere. A second behavior change on the
existing surface is a scope escape. See AC-S3.8.

### 6a. The question channel (ruling 5) — branch on STATUS, not on which id is present

> **María's ruling (thread `a14d9b29`, 2026-08-29) corrected this section twice.** My first draft
> picked the submit door by which id was present. That is wrong: there is a **third case with no id
> at all**, and a **fourth door that blocks the HTTP call**. Both are below.

**There are two different ways the server asks the phone something, and they are not the same door.**

| | **A — "I need an argument"** | **B — "I need a decision"** |
|---|---|---|
| Trigger | `/api/v2/ask` can't extract a required arg | `notify()` / `ask_yes_no` / `ask_multiple_choice` / `converse` |
| Arrives as | the **HTTP body**: `status:'parked'` + `pending_id`, `answer` = the question | a **WS `notification_queue_update`** whose `notification.response_requested == true` |
| Answered via | `POST /api/v2/resume` `{pending_id, answer}` | `POST /api/notify/response` `{notification_id, response_value}` |
| Is the server blocked? | No — the pending entry just waits | **Yes.** `notifications.py:1401` holds an SSE stream on `asyncio.wait_for( event, timeout_seconds )` until the phone answers or the timeout substitutes `response_default` |
| Mobile today | ❌ no repo method at all | ⚠️ partly built — see below |

Verified: `rest/v2/flow.py:1372` `_speak()` sends a plain notification with **no `response_requested`**, so a parked question is *spoken* but carries no answer affordance. Channel A really is HTTP-only.

#### Door C — the near-match confirmation, which BLOCKS the ask (María, verified)

`_near_match_replay` / `_user_confirms` (`rest/v2/flow.py:865, 939`): when a question scores close to a
cached one but below exact, the flow asks the user *"Is that the same as: …?"* — a **yes/no
`response_requested` notification** — and **blocks the HTTP request thread waiting for the reply**:
`timeout_seconds = 30`, `retry_on_timeout=True, max_attempts=3, backoff_multiplier=2.0` ⇒ 30s at
minimum, ~210s worst case. The prompt defaults to **"no"** (a wrong replay is worse than a re-run),
and a confirmer that raises counts as a no.

🔴 **This is live on the dev server Rick uses.** `similarity confirmation enabled = true` sits in
`[Lupin: Development]` (`lupin-app.ini:497`); it is `false` only under `[Lupin: Testing]` (`:637`).

🔴 **And mobile's shared Dio gives up at 30 seconds** (`http_service.dart:45`,
`receiveTimeout = 30s`) — the same instant the *first* confirm attempt expires. So today the ask
would time out on the phone while the server is still retrying, and the confirm would always
default to "no" with the user never seeing the question.

**Two consequences the plan must carry:**
1. The Quick Ask ask-call needs its **own longer `receiveTimeout`** (per-request `Options`, not a
   global change — 30s is right for every other call).
2. The question surface must be able to **answer a WebSocket question while the phone's own ask
   call is still in flight**. That is not a nicety: without it the cache-replay path can never be
   confirmed. It is also the strongest possible argument for Rick's ruling 5.

#### Widget shape — bodies are status-blind and door-agnostic (Clayton, S4 review)

**The "one widget, four doors" claim holds, and the existing code is already the reason.** Every
body in `lib/shared/widgets/prompt_bodies.dart` takes only its own data plus an `onRespond`
callback — `YesNoPromptBody({ required onRespond })`, `OpenEndedPromptBody({ required onRespond })`,
`MultipleChoicePromptBody({ required options, required multi, required onRespond })`. **No body
knows an endpoint, a status, or a door.** It renders from `responseType` and hands a value back to
its host.

⇒ **Invariant, stated so it is not eroded later:** the bodies stay status-blind and door-agnostic.
**The status branch lives in the HOST**, which decides — after branching on `status` per the table
below — whether the value goes to `/api/notify/response`, `/api/v2/resume`, or nowhere
(`needs_input`). Adding a `status`, an endpoint, or a door parameter to a body is the shape
fracturing; that is what AC-S4.11 exists to prevent.

#### The six outcomes to branch on — `status` first, then the id

María: *"Branch on `status` first — parked vs needs_input vs response_requested — and treat
`needs_input` as 'server is telling you, not asking you.' Then use the id to pick the endpoint."*

| `status` | Meaning | Client action |
|---|---|---|
| `done` (+ `answer`) | replay served from cache, or inline executor | render + speak |
| `waiting` (+ `job_id`) | queued; answer comes over WS | the §3 correlation machinery |
| `parked` (+ `pending_id`) | Door A — needs an argument | prompt widget → `POST /api/v2/resume` |
| `needs_input` (**no id**) | **the third case.** `flow.py:365` hard-codes `interactive=False` on the `submit` path, so it never parks — this is the server *telling*, not *asking* | terminal card naming `args_missing`. **No answer affordance** — there is nothing to answer to |
| `rejected` / `failed` | refused or errored | error card |

Plus, orthogonally at any time: a **`response_requested` notification** (Door B, and Door C's
confirm is one of these) arriving over the WebSocket.

**Rick's ruling is channel B**, and it is the one that blocks. The good news is that mobile already has most of B:
`POST /api/notify/response` is already wired (`notification_repository.dart:42`), and yes/no (with Neither + `[comment: …]`), open-ended, and single-select multiple-choice already render inline in `focus_chat_pane.dart` via the shared `prompt_bodies.dart`. **The surface exists; what's missing is the lifecycle** — precisely the blocking part Rick named.

**Round 1 scope for the question channel** — make the existing surface honest, then hang channel A on it:

1. **Lifecycle (the strategic gap).** `notification_expired` and `notification_responded` appear **zero times in `lib/`** — the frames arrive and are dropped. Add both dispatcher cases: expired → mark the ask dead and show "⏰ Expired — default used: X"; responded → another device/proxy answered, retire the card. Fold `state == 'expired'` into `FocusMessage.answered` (today an expired ask stays "pending" forever and poisons `pendingPromptFor`, so the composer tries to answer a dead ask and takes a 400 the user never sees). Add a countdown from `timeout_seconds` (parsed, never read) and use `response_default` (parsed, never read) for prefill and for a cancel-with-default. Branch the 400 on `"already responded"` / `"grace period exceeded"` instead of collapsing to a generic error.
2. **Fix the multi-question payload defect.** Mobile reads `response_options['options']` at the top level; the real wire nests `response_options.questions[]`, each with its own `question / header / multi_select / options`. So **a canonical `ask_multiple_choice` payload renders an empty option list on the phone today**, and the submitted value is a bare label rather than the `{"answers": {header: value}}` JSON the server parser expects. First cut: a simple one-question-at-a-time stepper, no Back/Next wizard.
3. **Hang Door A on the same widget.** Add `QueueRepository.resume( pendingId, answer )` → `POST /api/v2/resume`, and render a `parked` response using the *same* prompt body as a `response_requested` notification. One question UI; the **submit door is chosen after branching on `status`** per the table above, never by sniffing for an id. `needs_input` gets a terminal card with no answer box. This is what makes the widget generic rather than notification-specific — and it means "which city?" gets answered by voice in place, which is what Rick asked for.
4. **Survive Door C.** Give the ask-call its own `receiveTimeout` (≥ 240s, covering the ~210s worst-case confirm ladder) via per-request `Options` — never by loosening the global 30s, which is right for every other call. Keep the record button disabled but **the question surface live while an ask is in flight**, so the phone can answer the near-match confirm that its own ask is blocked on. Add a visible "waiting on your confirmation" state so a 30-second silence is explained rather than mysterious.

> 🔶 **OPEN DECISION FOR RICK — carry into the cascade, do not settle it in implementation.**
> María adds that `_near_match_replay` only landed **2026-08-03** and that traces from **08-15 to
> 08-28 show zero occurrences** — the blocking confirm has never actually fired. She suggests
> setting **`interactive=false` for mobile** instead of lengthening the timeout.
>
> **The tension:** the same `interactive` flag controls both behaviours. `interactive=false` skips
> the near-match confirm *and* stops the flow from parking on a missing argument (`flow.py:1250`) —
> so the phone could never ask "which city?" in place. That is exactly the shortcut Rick overruled
> in ruling 5.
>
> | Option | Keeps in-place follow-up | Handles Door C |
> |---|---|---|
> | **(a)** long timeout on the ask call only | ✅ | phone answers the confirm mid-flight |
> | **(b)** `interactive=false` for mobile | ❌ — contradicts ruling 5 | confirm never fires |
> | **(c)** ship (a), revisit if it ever bites | ✅ | zero occurrences in two weeks of traces |
>
> Plan is written against **(c)**, flagged as provisional pending Rick's ruling in the cascade.

**Deferred (named, not silently dropped):** the Back/Next multi-question wizard with "Question N of X"; the serialized ask queue (web keeps one active and defers the others' timers until promotion); localStorage-style survival across app restart; per-field mic buttons on the yes/no comment, batch questions and the "Other" field; the "Other" free-text option; prediction-hint rendering + 👍/👎 vote; a `human_only` badge; pause/resume of the countdown.

**Now:** one-live-question guard (a single `bool get canRecord`, so lifting it later is one line);
empty-transcript and mic-denied inline errors reusing `VoiceReplyField`'s shape; the watchdog;
`websocketId` from `WebSocketService.sessionId` with submission blocked when the socket is down.

⚠️ `asr_service.dart` warns that Whisper **hallucinates on silence** and returns non-empty junk — so
"send immediately" will occasionally fire a garbage question. Mitigate with cancel-on-the-live-card,
not a confirm dialog.

**Deferred to round 2:** the multi-job card window; cancel a live job (`cancelJob` exists);
background re-hydration off the existing `auth_success` seam; plus the question-channel items listed
at the end of §6a.

---

## Cascade-ready section breakdown

Per `planning-is-prompting → workflow/plan-review-cascaded-input-spec.md` §2a. Four sections, each
reviewable from its own prose plus the shared anchors (§Context, §Findings, §Decisions) — no section
requires loading a sibling's prose.

| § | Section | Covers | Rough size |
|---|---|---|---|
| **S1** 🎯 **hub — build first** | **Job-status correlation** | §1 `job_lifecycle.dart`, §2 card model, §3 `QuickAskBloc` (buffer / monotonic fold / watchdog / reconcile), §5 wiring | Largest |
| **S2** | **Capture & screen** | §4 screen, record button, scrollback ordering, ASR reuse, one-live-question guard, error surfaces | Medium |
| **S3** | **Speech** | §6 ownership + the `verbatim` flag, pause/resume/replay | Smallest |
| **S4** | **Generic question surface** | §6a in full — the four doors, status-first branching, lifecycle repairs, multi-question payload defect, resume door, Door C interlock | Large |

**Dependency DAG** — **S1 is a HUB, not a peer** (Pocholo + John, 2026-08-29; corrects my first draft,
which called S1 and S4 independent):

```
        ┌──▶ S4 ──┐
S1 ─────┤         ├──▶ S2
        └─────────┘
S3 ──────────────────▶ S2
```

| Edge | What flows |
|---|---|
| `S1 → S2` | the lane vocabulary S2's status chips render |
| `S1 → S4` | **the shared model + dispatcher surfaces.** S4's needs on `app.dart`, `queue_models.dart` and `queue_repository.dart` are a **strict superset** of S1's |
| `S3 → S2` | the replay/pause controls S2 mounts |
| `S4 → S2` | the prompt widget S2 shows inside the Quick Ask pane |

Acyclic; S1 and S3 are roots, S2 is the sink.

🔴 **Why this matters, and what my first draft got wrong.** I originally called S1 and S4
independent and gave S1 sole ownership of the three shared files. Pocholo's finding: since S1's need
is a **strict subset** of S4's, S1-owns means **S4 re-edits the same lines later** — the overlap
survives the ownership rule rather than being removed by it. The fix is not a different owner, it is
**sequencing: S1 lands first, S4 extends what S1 built.** Ownership rules resolve *concurrent*
contention; a subset relationship is not contention, it is an ordering.

**Independence and sequencing are different properties — do not conflate them.** All four sections
still pass the cold-reviewer test: S4's prose is reviewable without loading S1's. That is a *review*
property and the cascade's gate. Sequencing is an *implementation* property and constrains only the
build order. S4 stays concurrently reviewable while being sequenced second.

**Provides / consumes, stated both directions:**
- **S1 provides** `JobLifecycleState` / `JobLane` and the live-job state stream. **S2 consumes** them
  for the three status forms. S1 needs nothing from S2.
- **S3 provides** the `verbatim` enqueue flag and the pause/replay controls. **S2 consumes** them as
  buttons. S3 needs nothing from S2.
- **S4 provides** the prompt widget and its submit-door routing. **S2 consumes** it for the parked /
  confirm cases. S4 needs nothing from S2 — it also serves the existing focus surface.
- **S1 ↔ S4** are independent: S1 handles `job_state_transition`; S4 handles
  `notification_queue_update` questions and HTTP statuses. They meet only at S2.

**Open decision the cascade must resolve:** §6a.4 — the `interactive=false` vs long-timeout trade
(it changes S4's scope materially, and would partly reverse Rick's ruling 5).

**Scope note for reviewers:** S4 repairs an existing shared surface used by focus mode today, so its
blast radius is wider than the other three. The regression pin is that current notification speech
and prompt behavior stay byte-identical when the new flags are off.

## Open sub-questions

**OSQ1 — the `interactive` trade. ✅ CLOSED 2026-08-29 (Rick, via Pocholo).**
**Ruling: long `receiveTimeout` on the ask call only — per-request ~240s, the global 30s stays.
`interactive=false` REJECTED**, because it would have taken back ruling 5 (the in-place argument
interview). The conditionality notes on AC-S4.6 and AC-S4.7 are struck: option (b) is dead, so both
are **satisfied rather than struck**.

> **Adopted with the ruling — Chloé's finding: the Door C notification is the WATCHDOG RESET.**
> Her trace shows the `response_requested` confirm is *the one thing that arrives in the window where
> no job id exists yet*. That window is real and it is long: the ask blocks up to ~210s while the
> confirm ladder runs, and **no `job_state_transition` can arrive because nothing has been queued**.
> A watchdog armed at submission would fire into that silence with **nothing to reconcile** — there
> is no `job_id` to look up, so every probe would come back "not found" and the UI would declare
> `lost` on a request that is alive and waiting for the user.
>
> ⇒ **The watchdog treats a `response_requested` notification as proof of life and resets its timer**,
> exactly as it does for an accepted transition frame. See AC-S1.4.
>
> **Cross-section note:** this is an S1 mechanism driven by an S4 signal, and the only such coupling
> in the plan. S1 owns the reset; S4 owns the notification that triggers it.

**OSQ2 — who owns the cross-repo drift guard. ✅ CLOSED 2026-08-29 (Rick, via Pocholo).**
**Ruling: a parent-repo parity test. Fixture-only pinning REJECTED.** AC-G4 remains a **whole-plan
gate, not dischargeable from this repo**, and 🔴 **requires a parent-side owner who is not in this
cast** — stated here and on AC-G4's own line so it is not discovered at merge time.

**OSQ3 — should `verbatim` bypass the stop-list? ✅ CLOSED 2026-08-29 (Rick, direct keypress).**
**Ruling: NO — the stop-list holds. Suppression is made VISIBLE.** Nothing is spoken; the text is
always rendered on screen; the matching rule is named; one tap speaks it anyway. **The plan as
written stands — AC-S3.7 and AC-S4.14 are unchanged**, and `verbatim` continues to bypass gates 2
and 3 only.
> **Provenance — this is a REAL answer, not a default.** Asked three times. The first two returned
> `[default used] yes` (nobody home); the third returned `answered: true, default_used: false`. Only
> the third is a ruling. The first two are recorded here because the plan's written position matched
> the default, and a coincidence that looks like a ruling is exactly the defect that left five
> earlier decisions carrying a permanent provenance caveat.
> **Rick's own framing, which lowers the stakes rather than raising them** (verbatim): his stop-list
> exists to stop *rendering* event-spawned repetitive notification text in the focus-bar UI, so an
> overlap with a deliberately-asked answer is *"astronomically unlikely… in any way shape or form."*
> ⇒ **Do not build elaborate machinery for this path.** The visible-suppression notice is required
> for correctness (a silently dropped answer is indistinguishable from a hang) and is expected to
> fire approximately never. Cheap and correct beats thorough here.

**(original framing)**
Ruling 4 overrides two
*preference* gates. The stop-list is different in kind — a specific "never speak this" pattern the
user typed. The plan is written **not** to bypass it, and to make the suppression visible instead
(AC-S3.7). If Rick would rather a deliberately-asked answer always win over a stop-list match, this
flips to a bypass and AC-S3.7 becomes a bypass test. **Low stakes either way; it is a question about
whose instruction wins, not about mechanism.**

## Acceptance criteria

Every criterion is numbered and carries an `EXECUTOR` tag. **All mobile suites are `:7999`-class**
(fast, no persistent state, no monopoly) so every AC below is `EXECUTOR: AI`. Rick's device pass is
listed separately at the end and is deliberately *not* an acceptance gate.

### S1 — Job-status correlation

- **AC-S1.1** — `JobLifecycleState` agrees with a committed fixture of the server's
  `STATE_TO_UI_CONTAINER`, asserted **bidirectionally**: (a) every fixture key parses to an enum
  member with the fixture's lane, **and** (b) every enum member appears as a fixture key. Includes
  `stalled → todo`; unknown input returns null. `EXECUTOR: AI`
  > **Both directions are load-bearing.** (a) alone passes when the Dart enum has grown a member the
  > server does not have; (b) alone passes when the fixture has a key Dart cannot parse. Only the
  > pair catches both. **Scope limit:** this pins Dart against a *fixture*. It cannot detect the
  > fixture itself going stale against the live server — that is AC-G4, deliberately not an S1
  > criterion.
- **AC-S1.2** — Frames arriving **before** the `AskResponse` resolves are buffered and folded: a test
  that dispatches `pending→queued` and `queued→running` while `ask()` is still pending ends in
  `running`. `EXECUTOR: AI`
- **AC-S1.3** — The fold is rank-monotonic: `completed` then `running` stays `completed`; a duplicate
  `completed` emits one state; a frame for another user's `job_id` changes nothing. `EXECUTOR: AI`
- **AC-S1.3b** — **Foreign frames cannot evict ours from the pre-attribution buffer** (Pocholo's
  finding). A test floods the buffer with **more than its cap** of frames carrying a different
  `question_text` and `session_id`, interleaved with our own `pending→queued`, all **before** `ask()`
  resolves — our frame must survive the drain and fold. This pins insert-time filtering specifically:
  a drain-time-only implementation fails it. `EXECUTOR: AI`
  **Falsified by:** a buffer that appends every frame and filters at drain — passes a naive
  "our frame is folded" test, fails this one.
- **AC-S1.4** — After 45s of silence the watchdog reconciles via `getQueue('done'|'dead'|'run'|'todo')`
  and **never** calls `getJobHistoryEntry`; found-in-done completes with `responseText`, found-in-dead
  fails with `error`. Verified under `fakeAsync`, with a call-assertion on the forbidden method.
  `EXECUTOR: AI`
  > **Rewritten because the original was hollow** (Pocholo's suspicion, confirmed on re-reading). It
  > said "absent after 3 probes yields `lost`" without distinguishing *found-alive* from
  > *found-nowhere*, so **an implementation with the F-Chloé-S1-8 bug in it would have passed.** The
  > discriminating cases are now AC-S1.4c and AC-S1.4d, which fail against that implementation.
- **AC-S1.4c** — **Found-alive RESETS the ladder; only found-nowhere decrements** (F-Chloé-S1-8).
  Under `fakeAsync`: with `getQueue('run')` returning our job on **every** probe, the bloc must
  **never** reach `lost` — driven past the buggy implementation's death point.
  🔴 **The test DERIVES that point from the cadence constants; it does not hardcode 315s** (Pocholo).
  315 is only `sum(45, 90, 180)` for today's ladder — retune it to 60/120/240 and the death point
  moves to 420s, at which point a hardcoded 315 stops discriminating and the test **passes for the
  wrong reason**. Compute the drive-past horizon from the same constants the implementation uses.
  Separately, **exactly 3** consecutive *found-nowhere* results reach `lost` — **N = 3, bounded, not
  open-ended** — and only after spanning **more than 120s** (`cj flow consumer stall threshold
  seconds`), so we never declare lost while the server still considers the job healthy.
  Arithmetic for today's ladder: strikes land at 45s, 135s and 315s, so the 120s floor is cleared by
  strike 2 and N = 3 carries margin. **Both the floor check and the test window are computed from the
  cadence constants, never from these literals** — they are an illustration, not the source.
  `EXECUTOR: AI`
  > 🔴 **This bound went MISSING in `e4e0946` and was restored here** (John, via Pocholo). The
  > pre-revision text said "absent after 3 probes"; my rewrite replaced it with an unbounded "N
  > consecutive". **A revision introduced a defect while fixing others** — a watchdog that can never
  > terminate is a worse failure than the one I was repairing, since the UI would spin forever
  > instead of resolving wrongly. It was caught only because the section was pinned to a sha and
  > re-read against the previous one. **When rewriting an AC, diff it against what it replaced;
  > "clearer" is not the same as "complete".**
  **Falsified by:** the F-Chloé-S1-8 implementation itself — one shared rearm path where
  found-alive decrements. It passes the original AC-S1.4 and goes red here at 315s.
- **AC-S1.4d** — **Reconcile-vs-folded precedence** (F-Chloé-S1-8, second half). A terminal reconcile
  result applied over a non-terminal folded state wins; a non-terminal reconcile result applied over a
  terminal folded state is ignored — a `completed` frame already received is not undone by a stale
  listing. `EXECUTOR: AI`
  **Falsified by:** a reconciler that applies its result unconditionally — it overwrites a received
  `completed` with a stale `run` listing and goes red here.
- **AC-S1.4b** — **A `response_requested` notification resets the watchdog** exactly as an accepted
  transition frame does (Chloé's finding, adopted with the OSQ1 ruling). Pinned specifically for the
  **pre-job-id window**: a test that delivers a Door C confirm while the ask is still blocked and no
  `job_id` exists must show the watchdog rearmed and **no reconcile attempted** — there is nothing to
  look up, and probing would yield a false `lost` on a live request. `EXECUTOR: AI`
- **AC-S1.5** — `AskResponse` reports `waiting` distinctly: `isWaiting` is true and `summary` does not
  say "Done" for a queued body (the `queue_models.dart:219-221` gap Pocholo cited). `EXECUTOR: AI`
- **AC-S1.6** — The ask request carries `websocket_id` = the live `WebSocketService.sessionId`, never
  the literal `'mobile'`; asserted by capturing the request body through `StubAdapter`. `EXECUTOR: AI`
- **AC-S1.7** — `WsBlocDispatcher` routes `job_state_transition` to `QuickAskBloc`, and unknown frame
  types still drop silently; asserted against the real wiring in a service-integration test.
  `EXECUTOR: AI`

- **AC-S1.8** — **`WebSocketService` exposes an OBSERVABLE connection state** (Pocholo's manager
  ruling: the file had **no owner in any section**, and AC-S2.2a needs new API on it). A broadcast
  `Stream<bool>` emits at every connection transition — **all five** sites that mutate `_isConnected`
  (`websocket_service.dart:120, 142, 279, 292, 365`) each publish.
  🔴 **It MUST emit its current value on subscribe** (John): otherwise a screen mounted while
  *already* disconnected never learns, which is the same silent staleness that started this thread —
  the fix would carry the bug it was written to remove.
  🔴 **And it MUST emit at ALL FIVE assignment sites, not just connect and disconnect** (Clayton;
  I had written "six", counting the field declaration at `:20`, which is not a mutation — **five** is
  right: `:120, :142, :279, :292, :365`). **A stream wired at two of five is WORSE than no stream**:
  it looks observable and silently is not, which is the precise failure the clause exists to prevent.
  🔴 **AND distinct-until-changed** (Clayton's amendment). Three of the five sites set `false`
  (`:279, :292, :365`), so without it a single disconnect can emit `false` repeatedly. **Mirror
  `pausedStream`'s contract rather than inventing one** — its de-duplication lives at the *mutation
  site* (`tts_orchestrator.dart:166, 174`: `if ( _paused ) return;` before the `add`), which is the
  pattern to copy at each of the five sites.

  > ⚠️ **"Mirror `pausedStream`" is NECESSARY BUT NOT SUFFICIENT — I checked it rather than adopt the
  > wording.** `pausedStream` is a plain `StreamController<bool>.broadcast()` (`:74`) with **no
  > current-value replay on subscribe**. So mirroring it gives distinct-until-changed and **not**
  > emit-on-subscribe. AC-S1.8 needs **all three**: emit at all five sites · distinct-until-changed
  > at the mutation site (the `pausedStream` pattern) · **plus** a current-value replay that
  > `pausedStream` does not have. Taken literally, the amendment would have hardened the wrong half
  > and dropped John's.
  ⚠️ **Shared-service status, stated on the line the way AC-G4 carries its cross-repo status:** this
  is **new API on a service every feature uses**. S1 owns it (S1 already owns the `WsBlocDispatcher`
  switch and the lifecycle vocabulary, and connection state is transport-adjacent to both); **S2 and
  S4 consume**. `EXECUTOR: AI`
  **Falsified by:** a stream that emits only on *change* — a listener attached while disconnected
  gets nothing and the button stays enabled, exactly the failure this AC exists to prevent.
  > **Verified, not inferred** (John): `isConnected` is a plain getter over a private `bool`
  > (`:20`, `:31`), and the file's only `StreamController` carries **messages, not connection state**
  > (`:16`). So moving the predicate alone does **not** fix that clause — the stream does.

### S2 — Capture & screen

- **AC-S2.1** — Hold-then-release fires **exactly one** submit, including when the gesture is
  cancelled mid-press. `EXECUTOR: AI`
- **AC-S2.2a** — **`canRecord` is correct as a predicate** (Chloé's S2 finding, split half 1).
  Exercised against a **REAL `QuickAskBloc`**, never a `MockBloc`: each falsifying clause is driven
  independently, the predicate goes false for that clause and no other, and the reason it exposes
  names that clause. `EXECUTOR: AI`
  **Falsified by:** an inverted clause, a stale clause, or a missing fourth clause — each turns
  exactly one case red. **None can be caught under `MockBloc`, which is the point of the split.**
  > **Provenance of this AC's falsifier** (Clayton's re-verification, closing the last
  > single-sourced claim set). It rests on **Chloé's four-owners analysis and Clayton's assignment-
  > site count**, *not* on the S2 reuse table. That table is not refuted — it assessed **reuse**,
  > while clauses (c) and (d) turn on **state observability**, which it never assessed. **A claim
  > true in its own scope, then used outside it** — the same shape as everything else tonight: an
  > assertion still standing while the ground under it moved. Cite the two sources above.
- **AC-S2.2b** — **The screen RENDERS `canRecord` correctly** (split half 2). Under `testApp()` +
  `MockBloc`, a state with `canRecord: false` disables the button and shows the supplied reason; true
  enables it. Rendering only — no claim about the predicate's correctness. `EXECUTOR: AI`

  > 🔴 **Why the split, and why this differs from the other hollow checks** (Chloé). The others were
  > tests that *happened* not to discriminate. **This one could not discriminate by construction**:
  > AC-S2.1–S2.6 route to a widget test built on `MockBloc`, while the old AC-S2.2 promised to drive
  > each falsifying clause independently. **Mocking the bloc replaces the computation** — the test
  > proves the button greys out when told to, and passes unchanged if the predicate is inverted,
  > stale, or missing a clause. Under the falsified-by rule: **name the stub that turns it red, and
  > there is none.**

  > 🔶 **Four clauses, four owners — the boundary ruling is Pocholo's.** Checked at HEAD:
  >
  > | Clause | Source today | Verdict |
  > |---|---|---|
  > | no live job | `QuickAskBloc._liveJobId` | ✅ already bloc-side |
  > | no unanswered prompt | `FocusChatState.pendingPromptFor` (`:217`) — a **different bloc**, S4's surface | reachable: the bloc already receives `response_requested` frames via the dispatcher, so it can track its own pending prompt rather than read another bloc's state |
  > | socket connected | `WebSocketService.isConnected` (`:31`) — a **sync getter over a private `bool` mutated at five sites (`:120, 142, 279, 292, 365`), with NO stream and NO notifier** | 🔴 **the sharp one.** A predicate over an unobservable bool **goes stale silently**: the socket drops, the button stays enabled until something unrelated repaints, and the screen never learns — defeating the "blocked with a stated reason" behavior now split across AC-S2.2a and AC-S1.8. **Needs a real observable** — a broadcast `Stream<bool>` on `WebSocketService` emitting at each transition |
  > | nothing recording / transcribing | **no source at all** — `AsrService` exposes no `isRecording`, no state, no stream | 🔴 **DOES NOT DISSOLVE — reinstated 2026-08-29 (Tiffany's ruling on F-Clayton-S2-D). See AC-S2.2b.** |
  >
  > 🔴 **The ASR clause was dissolved on reasoning that is sound for ONE caller, and there are TWO**
  > (Clayton, F-Clayton-S2-D; **re-verified in-tree by the manager before ruling, not taken on the
  > report**). The struck reasoning read: *"The bloc initiates `startRecording()` and
  > `stopAndTranscribe()`, so it already knows. Adding state to `AsrService` would invent a second
  > source of truth for something the caller owns."*
  >
  > **The second source of truth already exists — it is `_activePath`, and it is shared.** Verified at
  > HEAD: `AsrService` is a **lazy singleton** (`service_locator.dart:327`) holding ONE `AudioRecorder`
  > and ONE mutable `String? _activePath` (`asr_service.dart:49`), written unconditionally at `:77` by
  > `startRecording` and consumed at `:98` by `stopAndTranscribe` (`stopped ?? _activePath`). **Two
  > components share that instance**: focus mode's `VoiceReplyField` (`voice_reply_field.dart:30`, via
  > `focus_mode_screen.dart:50`) and Quick Ask's new button.
  >
  > **`QuickAskBloc` knowing what IT initiated does not tell it that `VoiceReplyField` is mid-recording
  > on the same recorder.** And `VoiceReplyField` is a four-phase tap-toggle **with a review step**, so
  > a recording survives navigation: start a reply in focus mode → open the drawer → hold the Quick Ask
  > button → `_activePath` is overwritten and the first capture is orphaned; whichever stop fires first
  > consumes the other's path. **Reachable by ordinary navigation, not an exotic race.**
  >
  > ⇒ **A guard held by one caller cannot see the other. The state goes where the recorder is.**
  >
  > ⇒ **My read: the predicate belongs on `QuickAskBloc` as derived state**, making AC-S2.2a an **S1**
  > criterion with S2 and S4 consuming. Only the socket clause needs new API; the ASR clause
  > dissolves; the other two are already reachable. **Pocholo rules — the ownership rule is his.**

- **AC-S2.2b** — **`AsrService` refuses a SECOND concurrent capture, and says so** (Tiffany's ruling
  on F-Clayton-S2-D, 2026-08-29). `AsrService` exposes `bool get isCapturing` (true iff a capture is
  active), and `startRecording()` **throws `AsrException` before touching the recorder** when one is
  already in flight. Quick Ask surfaces that as its stated reason, exactly like the other three
  blocked-with-a-reason clauses. `EXECUTOR: AI`
  > **Why the state lives on the service and not the bloc**: the recorder and `_activePath` are
  > singleton-scoped, so a per-caller guard is structurally blind to the other caller. One owner, one
  > flag, beside the thing it guards.
  > **Scope bar — this is a GUARD, not a feature.** Concurrent capture across the two screens stays
  > out of scope for round 1; this criterion only makes the collision *impossible and legible* instead
  > of silent and lossy. Do not build queueing, hand-off, or a second recorder.
  > **Falsified by:** an implementation that guards inside `QuickAskBloc` instead — start a capture in
  > focus mode's `VoiceReplyField`, then start one from Quick Ask: the bloc-side guard passes, the
  > singleton's `_activePath` is overwritten, and the first recording is orphaned. **Do not delete
  > this clause.**

- **AC-S2.3** — The record button renders as a fixed header with the scrollback below it, newest pair
  first (Rick's ruling 3). `EXECUTOR: AI`
- **AC-S2.4** — Status chips render from `JobLane` for todo / running / done, and a dead lane renders
  an error card. `EXECUTOR: AI`
- **AC-S2.5a** — **A REAL `AsrException` dispatches no submit** (seventh instance of the hollow
  shape; same split as AC-S2.2). Against a **real `QuickAskBloc`** with a stubbed `AsrService` that
  actually **throws** `AsrException` (empty transcript, permission denied), no submit reaches the
  repository and the bloc returns to idle. `EXECUTOR: AI`
  **Falsified by:** a bloc that catches the exception and submits anyway — **which passes the
  MockBloc version, because the mock never throws.** That was the hollowness: the old AC asserted the
  screen will not submit when *handed a state that already says error*, which no implementation gets
  wrong.
- **AC-S2.5b** — **The error renders inline** (split half 2). Under `testApp()` + `MockBloc`, an
  error state renders the inline message and returns the screen to idle. Rendering only.
  `EXECUTOR: AI`
> 🗄️ **AC-S2.6 is RETIRED** (Pocholo's ruling; it was "submission is blocked with a stated reason
> when the socket is not connected"). Post-`canRecord`-move it was **either a duplicate of AC-S2.2b
> or an S1 criterion, and nobody could discharge it either way**. Resolved by splitting it where it
> actually lives: the **clause** is AC-S2.2a, the **rendering** is AC-S2.2b, and the **observable it
> depends on** is AC-S1.8. No behavior is dropped — only the ambiguous home.

### S3 — Speech

- **AC-S3.1** — No `enqueueAlways` occurs on a `completed` frame from the Quick Ask path; with both
  `FocusChatBloc` and `QuickAskBloc` registered, one inbound notification frame produces **exactly
  one** enqueue (extends the AC-S2.8 single-dispatcher pin). `EXECUTOR: AI`
- **AC-S3.2** — A Quick Ask answer speaks in **full** and is **not** muted: the `verbatim` flag skips
  both `_formatSpeech`'s fraction cut and `_systemSenderMuted` (Rick's ruling 4). `EXECUTOR: AI`
- **AC-S3.3** — **Without** the flag, orchestrator behavior is byte-identical to today — the
  regression pin against silently changing everyone else's notification speech. `EXECUTOR: AI`
- **AC-S3.4** — Replay re-enqueues at `priority:'urgent'` with a `TtsSender` whose `isPersona` is
  true, so it plays from the start and works even when system senders are muted. `EXECUTOR: AI`
- **AC-S3.4b** — **Replay works WHILE PAUSED** (S3 review). With the orchestrator paused and an
  utterance held, tapping replay resumes and plays the answer from the beginning — it does not
  silently enqueue. `EXECUTOR: AI`
  **Falsified by:** a replay that only calls `enqueueAlways(priority:'urgent', …)` — the current
  code path. It enqueues and plays nothing, and the test asserts audio actually started.
- **AC-S3.8** — **S3's change to EXISTING focus-mode behavior is a closed list of one** (Chloé).
  Every `response_requested` notification speaks verbatim across the whole focus surface — that is
  the intended repair, not a Quick-Ask-only change. A test asserts an existing focus-mode question
  (no Quick Ask job in flight) now speaks in full; a second behavior change on this surface is a
  scope escape and fails. `EXECUTOR: AI`
  **Falsified by:** an implementation that scopes setter 2 to Quick Ask jobs "to keep focus mode
  unchanged" — the existing-question test then goes red, correctly.
- **AC-S3.9** — **Replay-implies-resume drains the WHOLE held backlog, and that is deliberate**
  (Chloé — nobody had named it). `resume()` (`tts_orchestrator.dart:171`) is global, so replaying one
  answer un-holds everything pause was holding. Consistent with pause already being global — one
  hold, one truth, the same reason AC-S3.5 renders from `pausedStream` rather than local state — and
  nothing was ever dropped, so the backlog is intact rather than surprising. The test asserts the
  held items do resume, so the behavior is pinned rather than discovered. `EXECUTOR: AI`
  **Falsified by:** a replay that resumes only its own utterance — leaves the rest held with no
  visible hold state, which is the genuinely confusing outcome.
- **AC-S3.7** — **ANY stop-list-suppressed item the user is expected to act on is VISIBLY
  suppressed, never silent — ANSWERS *and* QUESTIONS** (S3 review; scope widened by Chloé). The text
  is **suppressed observably rather than silently**: the orchestrator reports the suppression and the
  matching rule, so a caller can render it. `verbatim` does **not** bypass the stop-list.
  **The UI claims live where a test can see them** — AC-S4.14 (placement + answer affordance) and
  AC-S4.15 (the seam) — not here; an orchestrator test cannot see a card. `EXECUTOR: AI`
  **Falsified by:** relying on `enqueueAlways`'s early `return` with no UI signal — indistinguishable
  from a hang. **Also falsified by an implementation that covers answers only** — the original
  wording of this AC.
  > 🔴 **This AC said "answer", and that two-word scope REOPENED the hole AC-S3.6 had just closed**
  > (Chloé). The stop-list is **gate 1 and fires FIRST** (`tts_orchestrator.dart:255`), before
  > `verbatim` is ever consulted. So a `response_requested` **question** matching a stop-list rule is
  > dropped with no UI signal, and for a Door C confirm that reassembles the entire failure this
  > evening dismantled: never heard → server blocked ~210s → defaults to "no" → the replay never
  > happens. **Strictly worse than the answer case, because something is waiting on the user.**
  > A one-word scope fix closes what a two-word scope opened.
  >
  > 🔶 **And scope alone is only half of it — PLACEMENT crosses sections** (Pocholo, correcting his
  > own instruction). A suppressed **question**'s notice must render **in S4's prompt widget, where
  > the answer affordance would have been** — not in S3's answer card. A card that says "something
  > was muted" while offering no way to answer it is a marginal improvement over silence, not a fix,
  > and the thing is still blocking the server. Per the ownership rule: **S3 owns the gate and the
  > decision to suppress; S4 owns the surface the notice appears on.** S3 raises the requirement,
  > S4 carries the render — see AC-S4.14. This AC must not sit in S3 alone.
- **AC-S3.5** — The pause control renders from `TtsOrchestrator.pausedStream`, not local state:
  emitting `true` on the stream flips the toggle with no tap, and the focus screen's existing toggle
  stays in agreement. `EXECUTOR: AI`
- **AC-S3.5b** — **A pause control mounted while ALREADY paused shows the correct state**
  (spillover finding, raised from checking Clayton's `pausedStream` amendment). `pausedStream` is a
  plain broadcast controller with **no current-value replay** (`tts_orchestrator.dart:74`), so a
  widget subscribing while TTS is already held receives **nothing** until the next transition and
  renders "not paused" while the queue is held. The screen seeds from the synchronous `isPaused`
  getter at mount and then follows the stream. `EXECUTOR: AI`
  **Falsified by:** a control that subscribes without seeding — mount it with the orchestrator
  already paused and the toggle reads wrong, indefinitely.
  > **Same class as the socket clause, one surface over.** It is worth stating that this is a latent
  > gap in the EXISTING focus pause toggle too, not something S3 introduces — the plan does not fix
  > that surface, but AC-S3.5b stops Quick Ask from inheriting it.
- **AC-S3.6** — **A question the system is waiting on is spoken in full and is not mutable**
  (Pocholo/Clayton's finding). Any notification with `response_requested == true` is enqueued
  `verbatim`, so it bypasses both `_formatSpeech`'s fraction cut and `_systemSenderMuted` — pinned
  specifically for the persona-less `ask.flow@lupin.deepily.ai` sender with `speakSystemSenders`
  **off**, which is the configuration that silently loses it today. `EXECUTOR: AI`

  > 🔴 **This AC CLAIMED to cover Door A and it did NOT — a live false claim at HEAD** (John,
  > re-verified at `59e28ae`). The predicate keyed on `response_requested == true`, but `_speak`
  > dispatches an **`AsyncNotificationRequest`**, which is documented **fire-and-forget and does NOT
  > include `response_requested`** (`notification_models.py:736`). So a **Door A interview question
  > is a plain notification**: not spoken verbatim, still muted, and picked up by **neither** AC-S3.7
  > nor AC-S4.14. That is the "which city?" case — **Rick's ruling 5** — and **every one of AC-S4.12's
  > N round trips**. The AC that was supposed to close the question-audibility hole named a door it
  > could not reach.
  >
  > ⇒ **Second arm, keyed on the sender** (John's fix; stamping a persona server-side is already out
  > of scope). **Tighter than sender alone:** every flow question calls
  > `_speak( …, job_id=None, … )` (`flow.py:737`, `:1255`) while the **answer** path passes a real
  > `job_id` (`:1315`). So the arm is **`sender_id == "ask.flow@lupin.deepily.ai"` AND no `job_id`**,
  > which selects questions without sweeping in answers or the "New … job" ack.
  >
  > ⚠️ **Named so it is deliberate, not accidental:** this arm also catches the receptionist degrade
  > line (`:177`, also `job_id=None`). That is acceptable — it is user-facing speech the user is meant
  > to hear — but it is a widening, and it should be a decision rather than a surprise.
- **AC-S3.6b** — **A Door A interview question is spoken verbatim and is not muted** (the arm above).
  Pinned for a `_speak`-shaped notification: sender `ask.flow@lupin.deepily.ai`, **no
  `response_requested`**, **no `job_id`**, with `speakSystemSenders` **off**. `EXECUTOR: AI`
  **Falsified by:** a predicate keyed only on `response_requested` — i.e. AC-S3.6 exactly as it read
  at `59e28ae`. It passes the Door C case and goes red here.
  **Falsified by:** the current `enqueueAlways` path with no `verbatim` flag — the question is dropped
  by `_systemSenderMuted` and the test asserts nothing was spoken.

### S4 — Generic question surface

- **AC-S4.1** — Branching is on `status` first: **all SIX outcomes** route correctly — `done`,
  `waiting`, `parked`, `needs_input`, `expired`, `failed` (plus `rejected`). `EXECUTOR: AI`
  > **It read five; the server's own field says six** (F-Clayton-S4-1, verified at
  > `v2_ask.py:91` — `done | waiting | parked | needs_input | expired | failed`). The sixth,
  > **`expired`, is emitted only from `/api/v2/resume`** — the door AC-S4.8 opens — at `flow.py:698`
  > (`pending_expired`) and `:727` (`already_resumed`). Those are the **exact twins** of the two 400s
  > AC-S4.9 already handles on the notification door, so the plan handled the pair on one door and
  > not the other. Treated the same way: resolve the card with what happened, never a generic error.
- **AC-S4.2** — **`needs_input` with no id renders a terminal card and offers no answer affordance**
  (María's correction; the id-sniffing design would have offered a box with nowhere to send it).
  `EXECUTOR: AI`
  **Falsified by:** the original id-sniffing router — it renders an answer box for `needs_input` and
  goes red on the no-affordance assertion.
- **AC-S4.3** — `notification_expired` marks the ask dead and surfaces the default used;
  `notification_responded` retires it as answered elsewhere. Both dispatcher cases asserted against
  the real wiring. `EXECUTOR: AI`
- **AC-S4.4** — A row whose `state == 'expired'` counts as answered, so `pendingPromptFor` stops
  returning dead asks. `EXECUTOR: AI`
- **AC-S4.5** — A canonical `ask_multiple_choice` payload (options nested under
  `response_options.questions[]`) renders a **non-empty** option list and submits
  `{"answers": {header: value}}`, not a bare label. `EXECUTOR: AI`
- **AC-S4.6** — A `response_requested` yes/no arriving **while the phone's own ask is still in
  flight** is answerable, and answering it leaves the pending ask undisturbed — the Door C interlock.
  `EXECUTOR: AI` — OSQ1 closed in favour of the long timeout, so this stands as written
  (conditionality note struck 2026-08-29).
- **AC-S4.7** — The ask call carries its own `receiveTimeout` ≥ 240s while every other call keeps the
  global 30s; asserted on the captured request options. `EXECUTOR: AI` — OSQ1 closed in favour of the
  long timeout, so this stands as written (conditionality note struck 2026-08-29).
- **AC-S4.8** — `resume( pendingId, answer, websocketId, speak )` posts to `/api/v2/resume` with the
  correct body, and a `parked` response renders in the shared prompt widget. `EXECUTOR: AI`
  > 🔴 **FOUR fields, not two** (Clayton, verified at `rest/v2/routers/v2_ask.py` `ResumeRequest`):
  > `pending_id`, `answer`, `websocket_id`, `speak`. **`websocket_id` is how the answer's TTS gets
  > routed.** The ask turn sets it and the old two-arg signature dropped it on the resume turn, so the
  > second turn of one conversation would speak nowhere. AC-S4.12 folded the loop half of this
  > finding; this is the signature half.
  > **Falsified by:** a `resume` that omits `websocket_id` — the asserted request body then lacks the
  > routing field and the test goes red. **Do not delete this clause.**
- **AC-S4.9** — A 400 carrying `"already responded"` or `"grace period exceeded"` resolves the card
  instead of raising a generic error. `EXECUTOR: AI`
- **AC-S4.10a** — **The existing focus-mode prompt and notification tests pass UNMODIFIED**
  (Clayton's revision 2, regression half). The suite files that cover `focus_chat_pane.dart` prompt
  rendering and the notification-response path are not edited, not re-baselined and not quarantined
  as part of S4; they run against the changed shared surface and stay green. `EXECUTOR: AI`
  **The named files** (John: the falsifier could not run while "the named files" were never named) —
  `test/widget/focus_mode/focus_chat_pane_test.dart`, `test/unit/focus_mode/focus_chat_bloc_test.dart`,
  `test/unit/focus_mode/focus_chat_state_test.dart`, `test/unit/notifications/notification_bloc_test.dart`,
  (**`test/widget/notifications/interactive_prompt_sheet_test.dart` was REMOVED from this frozen
  list 2026-08-29 — see AC-S4.10b delta 3; it is a pinned delta now, not a frozen file**). The check is
  `git diff --exit-code <pre-S4-sha> -- <those paths>`; any edit to them inside S4 fails it.
  **Falsified by:** any S4 commit that edits one of those test files to make it pass — the
  "unmodified" check is a diff against the pre-S4 revision of the named files, so a re-baseline goes
  red instead of passing quietly.
- **AC-S4.10b** — **The two intended focus-mode deltas are named and pinned** (Clayton's revision 2,
  delta half). S4 changes exactly two behaviors on the shared surface, and each has a test asserting
  the NEW behavior:
  (1) a canonical nested `ask_multiple_choice` payload renders inline chips in the focus pane instead
  of routing to the "Answer in full view…" fallback — today `optionsRenderable` is false at
  `focus_chat_pane.dart:346`, so the fallback is what a canonical payload gets;
  (2) a row with `state == 'expired'` counts as answered at `focus_chat_state.dart:52`, so
  `pendingPromptFor` stops returning it.
  (3) a canonical nested `ask_multiple_choice` payload renders a **non-empty** option list in
  `interactive_prompt_sheet.dart` — today it reads `options?["options"]` / `options?["multi_select"]`
  at `:74-77`, the SAME top-level-vs-nested defect AC-S4.5 repairs one layer up.
  A **fourth** behavior change on this surface, not listed here, is a scope escape and fails this
  criterion. `EXECUTOR: AI`
  > 🔴 **Why delta 3 exists — Rick's ruling 2026-08-29, on Clayton's finding.** The sheet is where
  > delta (1)'s **"Answer in full view" fallback lands**. Leaving it frozen meant a canonical payload
  > hit the same wall twice: inline chips repaired, and the escape hatch behind them still broken.
  > A file cannot be simultaneously frozen by 10a, required-to-change by S4.5, and absent from the
  > contested-surfaces table. It is now a named delta, its test moved out of 10a's frozen list, and
  > the file is listed as a contested surface.
  **Falsified by:** an implementation that gates either delta behind a flag to "keep focus mode
  unchanged" — both tests then go red, which is correct: the deltas are the repair, not a side effect
  of it.
  > **Why AC-S4.10 could not be edited in place** (Clayton). "Unchanged when the new flags are off"
  > named a mechanism S4 does not have — every flag in this plan is S3's `verbatim` or the server's
  > `interactive` from the closed OSQ1. Read literally, the old AC **forbade AC-S4.4 and AC-S4.5**,
  > both unconditional changes to focus-mode behavior, which is exactly what makes them repairs. And
  > 10b is not bookkeeping: it turns an open-ended blast radius into a **closed list**, so a third
  > accidental change stops being indistinguishable from the two intended ones.
- **AC-S4.15** — **The suppression SEAM is spanned end-to-end** (the cross-section risk Pocholo
  watched for all night). A single test drives a stop-list-matching `response_requested` notification
  **through the real wiring** — orchestrator suppression → prompt-widget notice with its answer
  affordance — mirroring `quick_ask_wiring_test.dart`'s solution for the dispatcher handoff.
  `EXECUTOR: AI`
  **Falsified by:** cutting the wire between the two halves. **AC-S3.7 and AC-S4.14 both stay GREEN
  while the user gets silence** — each half passes alone and the pair fails, which is precisely the
  failure the ownership rule was meant to prevent. **Splitting the AC did not split the risk.**
  > **Residual folded in with it** (John): AC-S3.7 was asserting **three UI claims from an
  > orchestrator test that cannot see a card**. Those claims move here and to AC-S4.14; AC-S3.7 keeps
  > only what the orchestrator can actually observe — that the item was suppressed and why.
- **AC-S4.14** — **A suppressed QUESTION's notice renders in the prompt widget, with its answer
  affordance intact** (S3 raises, S4 carries — Pocholo's placement correction to AC-S3.7). When the
  stop-list suppresses a `response_requested` item, the prompt widget shows the question text, states
  it was not spoken and which rule matched, offers speak-anyway, **and still renders the normal
  answer controls** — the user can answer it silently. `EXECUTOR: AI`
  **Falsified by:** rendering the notice in the answer card instead — the user is told something was
  muted and has no way to act on it, while the server stays blocked. The test asserts the answer
  affordance is present on the suppressed question.
- **AC-S4.12** — **The re-entrant interview works: N arguments, N round trips on ONE `pending_id`**
  (John. This *is* ruling 5 — "which city?" answered in place is a second parked turn). `flow.py:737`
  comments it verbatim — *"Interview continues — re-ask the next arg on the SAME pending_id"* — and
  calls `release_turn`, so the loop is legal and a three-argument question is three round trips. The
  test drives two consecutive parked turns: the client re-posts the **same** `pending_id`, a second
  `parked` loops back to the prompt widget rather than terminating, and `args_missing` shrinks by one
  each turn. `EXECUTOR: AI`
  **Falsified by:** a client that treats the first `resume` as terminal — it renders the answer card
  after turn one and never asks the second question, which is ruling 5 silently half-implemented.
- **AC-S4.13** — **The resume door's two outcomes are OWNED by name, not merely routed** (John:
  `expired` was routed, and "routes correctly" is undefined, so a generic error card satisfied
  AC-S4.1 while the real intent sat in a prose note). `pending_expired` (`flow.py:698`) and
  `already_resumed` (`:727`) are the **twins** of the two 400s AC-S4.9 owns on the notification door,
  and **they mean different things to the user**: expired = the question timed out, ask again;
  already-resumed = you (or another device) already answered this turn. Each renders its own message
  and neither is a generic error. `EXECUTOR: AI`
  **Falsified by:** one shared "something went wrong" card for both — passes AC-S4.1's "routes
  correctly" and goes red here.
- **AC-S4.11** — **Prompt bodies stay status-blind and door-agnostic** (Clayton's revision 1). Each
  body is exercised with only its data plus a stub `onRespond` and produces the correct value with
  **no repository, no status and no endpoint in scope**; a source-level check asserts
  `prompt_bodies.dart` imports no repository and names neither `/api/notify/response` nor
  `/api/v2/resume`. The host owns the status branch and the door choice. `EXECUTOR: AI`
  **Falsified by:** a body that takes a `status` or an endpoint parameter "just for the parked case" —
  renders correctly, and goes red on the source check.

### Whole-plan gates

- **AC-G1** — `flutter analyze` clean on every touched file. `EXECUTOR: AI`
- **AC-G2** — Full suite green with the count risen from today's **597 ✅ / 0 failures outside
  `legacy_quarantine/`**, and zero new failures. `EXECUTOR: AI`
- **AC-G3** — Live check against `:7999`: one cached question (expect **`path == "replay"` with
  reason `exact_hit`** on the v2 ask response — 🔴 **NOT `is_cache_hit`, which does not exist on that
  response**; it is a job-history/queue-row field, so assert it on the `getQueue` row if you want it
  too) and one cold
  conversational question; three transitions observed, answer rendered, pause / resume / replay
  exercised. `EXECUTOR: AI` (`:7999` is AI-discretionary per §TESTING VENUES — no user gate)

- **AC-G4 — cross-repo drift guard (WHOLE-PLAN GATE, not an S1 criterion).** A parity test asserts
  the mobile fixture matches the **live** `STATE_TO_UI_CONTAINER` in
  `lupin/src/cosa/rest/job_state.py`, bidirectionally, so a server-side state addition or lane change
  fails a test instead of silently diverging on the phone. Precedent for the shape:
  `test_external_scope_mount_parity.py`. `EXECUTOR: AI` — **🔴 NOT DISCHARGEABLE FROM THIS REPO, AND
  UNOWNED: needs a parent-side owner who is not in this cast.** OSQ2 closed in favour of the parity
  test (fixture-only pinning rejected), but closing the question did not staff it. Until a
  parent-side seat takes AC-G4, AC-S1.1 pins Dart to a fixture and **nothing pins the fixture**.

  > **Why this cannot be an S1 criterion** (Pocholo, 2026-08-29): a mobile section can only assert
  > things inside the mobile repo. Drift is a property of the *pair* of repos. An AC that claims to
  > catch drift while living on one side of it is a tag that cannot do what it says.

**Not an acceptance gate:** Rick's on-device pass (laptop APK rebuild per
`src/rnd/2026.06.12-poc-laptop-build-runbook.md`) to judge whether the button *feels* right in the
hand. `EXECUTOR: HUMAN` — **reason: subjective ergonomics of a press-and-hold gesture on real
hardware; there is no automatable assertion for "feels right".** Every functional behavior above is
covered by an `EXECUTOR: AI` criterion, so nothing is deferred to Rick for correctness.

## Contested surfaces — ownership

Files touched by more than one section. **The owning section makes the edit; the consuming section
states its requirement and does not touch the file.** This keeps concurrent section work from
colliding.

**Three of these are SEQUENCED, not owned.** Where S4's need is a strict superset of S1's, an
ownership rule cannot remove the overlap — only build order can. **S1 lands its subset first; S4 then
extends the same file.** No concurrent edit, so no contention to arbitrate.

| Surface | Resolution | Note |
|---|---|---|
| `lib/app.dart` (`WsBlocDispatcher`) | **SEQUENCED — S1 first, then S4** | S1 adds the `job_state_transition` case. S4 *later* adds `notification_expired` / `notification_responded` + the `notif.jobId` belt line. Distinct `case` arms in one switch — additive, and non-overlapping once ordered. |
| `lib/features/queue/data/queue_models.dart` | **SEQUENCED — S1 first, then S4** | S1 lands the `waiting` gap at `:219-221` (Pocholo's citation). S4 then adds the `parked` / `needs_input` branch on the same `AskResponse`. |
| `lib/features/queue/data/queue_repository.dart` | **SEQUENCED — S1 first, then S4** | S1 lands the ask-call changes incl. the per-request timeout; S4 then adds `resume()`. Separate methods on one class. |
| `lib/services/tts/tts_orchestrator.dart` | **S3** | S2 | The `verbatim` flag and pause/replay semantics. |
| `lib/features/focus_mode/domain/focus_chat_bloc.dart` | **S4** | S3 | S4 owns the ask-lifecycle bloc; S3 states where the `verbatim` flag must be set and S4 makes that edit. |
| `lib/features/notifications/presentation/interactive_prompt_sheet.dart` | **S4** | S2 | The "Answer in full view" fallback destination; carries the same nested-payload defect as `prompt_bodies.dart`. Pinned by AC-S4.10b delta 3 (added 2026-08-29 — it was frozen, required-to-change and unowned at once). |
| `lib/shared/widgets/prompt_bodies.dart`, `focus_chat_pane.dart` | **S4** | S2 | Shared with focus mode today — widest blast radius, pinned by AC-S4.10a (no regression) + AC-S4.10b (the two intended deltas, as a closed list). |
| `lib/core/testing/test_keys.dart` | **additive, no owner** | all | Append-only constant blocks; each section adds its own prefix. Conflict risk is textual, not semantic. |
| `lib/core/di/service_locator.dart`, `MultiBlocProvider` | **S1** | S2 | `QuickAskBloc` registration + eager construction. |
| `test/service_integration/focus_app_wiring_test.dart` | **S3** | S1, S4 | Holds the single-dispatcher pin; S3 extends it, others state requirements. |

## Verification

**This section is a MAP, not a second source of truth.** Each entry names the test file and the
**AC ids it discharges**; the behavior itself is specified once, in the Acceptance Criteria. If an
entry here and an AC disagree, **the AC wins** — and the disagreement is a bug in this section.

> **Why the indirection** (Pocholo, 2026-08-29): this section was byte-for-byte the pre-AC draft and
> had already drifted — its first bullet still claimed `job_lifecycle_test.dart` pins Dart against
> `STATE_TO_UI_CONTAINER`, the exact claim AC-S1.1 was rewritten to retire. Two prose copies of the
> same requirement drift the moment one is edited. Naming AC ids instead of restating behavior means
> the next edit has one place to land.

All mobile suites are `:7999`-class (fast, no persistent state, no monopoly) and **AI-run — Rick is
never the tester.**

| Test file | Discharges | Notes |
|---|---|---|
| `test/unit/queue/job_lifecycle_test.dart` | **AC-S1.1** | Bidirectional against the committed fixture, both directions asserted separately |
| `test/unit/quick_ask/quick_ask_bloc_test.dart` (mocktail + bloc_test + `fakeAsync`) | **AC-S1.8, AC-S2.2a, AC-S2.5a, AC-S1.2, AC-S1.3, AC-S1.3b, AC-S1.4, AC-S1.4b, AC-S1.4c, AC-S1.4d, AC-S4.1, AC-S4.2** | Includes the pre-attribution race, the monotonic fold, the watchdog ladder, the forbidden-call assertion, and the six-outcome status table |
| `test/unit/queue/queue_repository_test.dart` (extend; `StubAdapter` + `makeDio`) | **AC-S1.5, AC-S1.6, AC-S4.7, AC-S4.8** | Request bodies and per-request options captured and asserted |
| `test/service_integration/quick_ask_wiring_test.dart` (NEW) | **AC-S1.7, AC-S3.1, AC-S4.3** | Mirrors `focus_app_wiring_test.dart` against the REAL wiring. That test registers only two blocs in GetIt — register `QuickAskBloc` in its `setUp` rather than guarding with `isRegistered` in production code |
| `test/widget/quick_ask/quick_ask_screen_test.dart` (NEW; `testApp()` + `MockBloc`) | **AC-S2.1, AC-S2.2b, AC-S2.3, AC-S2.4, AC-S2.5b, AC-S3.5, AC-S3.5b** | |
| `test/widget/quick_ask/door_c_interlock_test.dart` (NEW) | **AC-S4.6** | The regression that would otherwise silently return to "confirm always defaults to no" |
| `test/unit/notifications/ask_lifecycle_test.dart` (NEW) | **AC-S4.3, AC-S4.4, AC-S4.9** | |
| `test/unit/notifications/prompt_payload_test.dart` (NEW) | **AC-S4.5, AC-S4.11** |
| `test/widget/notifications/suppressed_prompt_test.dart` (NEW) | **AC-S4.14** |
| `test/service_integration/suppression_seam_test.dart` (NEW) | **AC-S4.15** | Spans orchestrator suppression → prompt-widget notice through the REAL wiring; mirrors `quick_ask_wiring_test.dart` | The suppressed-question notice renders in the prompt widget WITH its answer affordance |
| `test/unit/quick_ask/resume_interview_test.dart` (NEW) | **AC-S4.12, AC-S4.13** | Two consecutive parked turns on one `pending_id`; each resume-door outcome renders its own message | |
| `test/unit/services/tts_orchestrator_test.dart` (extend) | **AC-S3.2, AC-S3.3, AC-S3.4, AC-S3.4b, AC-S3.6, AC-S3.6b, AC-S3.7, AC-S3.8, AC-S3.9** | S3.3 is the byte-identical-when-off pin |
| existing focus-mode prompt + notification tests (unchanged, must stay green) | **AC-S3.3, AC-S4.10a, AC-S4.10b** | The blast-radius pin for the shared surface |

**Fixtures**
- `test/fixtures/queue/job_state_transition_*.json` — captured verbatim from
  `running_fifo_queue.py:1691` (completed), `:1356` (stalled), and the failed path. Pin the **full**
  metadata shape, not just `response_text` — that fixture is what protects round 2's card view.
- `test/fixtures/queue/state_to_ui_container.json` — the mirrored server map AC-S1.1 asserts against
  bidirectionally. **Nothing pins this fixture until OSQ2 closes** (AC-G4).

**Whole-plan gates** — **AC-G1** (`flutter analyze` clean on touched files), **AC-G2** (full suite
green, count up from today's 597 ✅ / 0 failures outside `legacy_quarantine/`, zero red), **AC-G3**
(live `:7999` check: one cached question expecting `is_cache_hit`, one cold conversational question,
three transitions observed, answer rendered, pause / resume / replay exercised).

**AC-G4** is **not dischargeable from this repo** — the parity test must live in the parent Lupin
repo. Blocked on **OSQ2**.

**Coverage check (run before declaring the plan verified).** Every AC id must appear at least
TWICE in this document — once where it is defined, once where it is discharged. Ids are written in
full (`AC-S2.4`, never a `S2.1 – S2.6` range) precisely so this is a runnable grep rather than a
prose promise. `EXECUTOR: AI`

```bash
DOC=src/rnd/2026.08.29-quick-ask-push-to-talk-screen.md

# Ids are DERIVED from the AC definitions, never hand-listed.
for id in $( grep -oE '^- \*\*AC-[A-Za-z0-9.]+' "$DOC" | sed 's/^- \*\*AC-//' | sort -u ); do
  esc=$( printf '%s' "$id" | sed 's/\./\\./g' )
  # Trailing class is a WORD BOUNDARY: without it "S4.10" substring-matches its
  # own children "S4.10a"/"S4.10b" and a RETIRED id reports clean.
  n=$( grep -oE "AC-${esc}([^A-Za-z0-9]|$)" "$DOC" | wc -l )
  [ "$n" -lt 2 ] && echo "UNDISCHARGED: AC-$id ($n)"
done
```

> 🔴 **This control failed its own case, and the failure was invisible** (John, S4 Stage 3). Two
> defects at once: the id list was **hand-maintained and froze at 32 while the document defined 41**,
> so **ten criteria were never checked at all**; and it still listed the retired `S4.10`, which
> **substring-matches its children `S4.10a`/`S4.10b`** and therefore reported clean on a dead id.
> **A green from a stale list is worse than no check** — it is the sixth instance tonight of the
> exact pattern this control exists to prevent, occurring *inside the control*.
>
> Both causes are structural and both are now removed: **ids are derived from the definitions**, so
> the list cannot drift; and the match is **word-boundary anchored**, so a retired parent cannot hide
> behind its children. Applying the falsified-by rule to the control itself is what names the input
> that turns it red: **a retired id that substring-matches a live one.**

> ### 🔴 STANDING RULE — an AC and its test must be able to FAIL TOGETHER
>
> **The check: name the stub that would make the test go red.** If you cannot name an implementation
> that satisfies the AC's words while failing its intent — or worse, one that carries the known bug
> and still passes — the AC is decorative and the `EXECUTOR: AI` tag on it is a claim, not a check.
>
> **Three instances in one evening say this needs writing down, not remembering:**
>
> | Hollow thing | How it passed anyway | Caught by |
> |---|---|---|
> | The coverage check | prose over shorthand ranges — 15 ids read as undischarged the first time it was actually run | running it |
> | AC-S1.4 (original) | said "absent after 3 probes yields `lost`" without distinguishing *found-alive* from *found-nowhere*, so an implementation with F-Chloé-S1-8 in it **passed** | John's read, confirmed by re-checking |
> | The buffer fix in `ffa98fa` | a text-only insert filter **narrowed** rather than closed — another user asking the identical question still got through | re-checking instead of trusting the fix |
>
> **All three were defects in work that had already been reviewed and reported as done.** The habit
> that caught them is the same one each time: **run the check instead of trusting it.**
>
> ⇒ ACs below carry an explicit **falsified by:** clause wherever the hollow shape is plausible —
> naming the implementation that makes the test go red. Do not delete those clauses; they are the
> proof the test discriminates.

> **Why the ids are written in full, and why this block exists at all.** The first version of this
> check was prose ("every AC id appears at least once") over a table using shorthand ranges like
> `AC-S2.1 – S2.6`. When it was actually run, **15 ids came back undischarged** — the check could not
> do what its `EXECUTOR: AI` tag claimed. That is the hollow-tag failure Convention 3 exists to catch,
> and the check caught it in the work that wrote it. **Do not re-compress these ids into ranges.**
> A verification step you have not executed is a claim, not a check.

**Stale-cross-reference check (run after ANY id rename, split, or count change).** Three separate
stale references survived edits that were themselves correct — a retired id after the split, and two
outcome-count notes after the sixth was found. **A fix that lands correctly still leaves references
to what it replaced**, and prose references do not fail a build. `EXECUTOR: AI`

Every line in this block that would match its own greps is tagged `selfref`, and the greps exclude
that tag. **The exclusion is implemented, not merely described** — an earlier version assigned a
`BODY=$(...)` variable and never used it, so the check returned three hits while claiming one was
clean (Clayton). *A check that reports hits you are told to ignore trains you to ignore the check.*

```bash
DOC=src/rnd/2026.08.29-quick-ask-push-to-talk-screen.md

# 1. references to a retired AC id                                    # selfref
for dead in "AC-S4\.10" "AC-S2\.6"; do                                # selfref
  grep -nE "${dead}([^A-Za-z0-9]|$)" "$DOC" | grep -v selfref          # selfref
done
# expect ONLY the historical note for each retired id. Add every retirement here —
# a retired id with no entry is a stale reference nobody is looking for.

# 2. stale outcome-count claims                                       # selfref
grep -nE "five outcomes|five-outcome|all five (outcomes|paths|of the outcomes)" "$DOC" | grep -v selfref   # selfref
# expect ZERO lines.
# NOTE: the bare pattern "all five" was RETIRED 2026-08-29 — it matched the
# `_isConnected` "all five sites" lines (AC-S1.8), which are CORRECT and have
# nothing to do with outcome counts. The check reported two hits you were told
# to ignore, which is the exact failure mode this block warns about one
# paragraph up. A different five is not a stale five.
```

```bash
# 3. the exclusion tag must not leak outside this block                # selfref
awk '/^\*\*Stale-cross-reference check/{i=1} /^Generalise it:/{i=0} /selfref/{print NR": "(i?"ok":"LEAKED")}' "$DOC"
# expect every line "ok". A LEAKED tag sits outside the block and would silence a
# REAL stale reference (Clayton's residual risk on the tag's scope).
```

**Clean = one hit from check 1, zero from check 2, no LEAKED from check 3.** Anything else is a stale
reference — or a tag that has escaped and is hiding one.

> ⚠️ **These greps have a LINE-WRAP BLIND SPOT, and it has already bitten twice** (John's method
> note). A claim split across two wrapped lines is invisible to a single-line `grep` — his first pass
> missed the §6 prose that way, and it is **how he nearly reported AC-S3.6 fixed when it was not.**
> ⇒ **A grep proves presence, never absence.** Confirming that a claim is *gone*, or that prose says
> what you think it says, requires **reading the passage**. Use these checks to find candidates, not
> to close a question.

Generalise it: **after renaming or splitting an AC, grep the whole document for the old id and for
any sentence that restates a count.** The edit is not finished when the definition is right; it is
finished when nothing still points at the old one.

**AC-G4 is expected to appear only in its definition and the OSQ2 note** — it is not dischargeable
from this repo, so its absence from the test table is correct, not a gap.

**Not a gate — Rick's device pass:** specified once in §Acceptance criteria (the sole
`EXECUTOR: HUMAN` item, deliberately outside the gates). Not restated here — that second prose copy
is the drift this section was just rewritten to remove.
