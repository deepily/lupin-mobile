# 2026.09.18 — Emulator UI check list

These are the UI changes from 2026-09-16 and 2026-09-17 that no one has seen running yet. **All eleven items come from one rebuild** (`src/scripts/build-and-deploy-lupin-mobile.sh`).

**Emulator mic**: turn on *Extended controls → Microphone → Virtual microphone uses host audio input*. Without it the emulator records silence (the 09-16 finding).

| # | Where | Do | Expect | Commit |
|---|---|---|---|---|
| 1 | Sign-in | Enter a wrong password and sign in | The password is still in the field, and the 👁️ eye shows and hides it | `bc98024` |
| 2 | Focus → a seat | Look at the voice-reply row | The buttons are taller (60 dp) | `bc98024` |
| 3 | Focus rail | Cold start, then tap **Refresh sessions** in the toolbar | Every live seat is listed, including one that has never written to you | `cf5280a` |
| 4 | Focus → a quiet seat | Type a message and send it | The seat moves onto the Live rail. The `:7999` log shows `POST /api/notify` with `direction=human_to_ai` (closes lupin `80f10bdd`) | `cfb8285` `6dba6fc` |
| 5 | Focus pane | Read a few messages | Bubbles take about 90% of the width. Each accent bar is the **sender's** colour, not orange. The header shows the persona name in bold and the sender id in italics | `c9a3375` `d7aaacd` |
| 6 | Focus → a message with a doc link | Tap the link, portrait then landscape | Portrait: the conversation and the document are stacked, half each. Landscape (≥600 dp wide): they sit side by side, half each | `ceebacc` `91b2139` |
| 7 | Focus composer | Record a spoken reply | The review box is full width with up to 8 lines, and its buttons sit below it | `fc8d9dc` |
| 8 | Focus composer | Record silence | You see *"Did not catch anything — tap the microphone and try again."* instead of an empty box with a live Send | `fc8d9dc` |
| 9 | Quick Ask, with *Send immediately* **off** | Ask a question by voice | The log's `[HTTP] Request: POST` line shows `/api/v2/transcribe` (closes `c3fc62bf`) | `a2f1d75` |
| 10 | Focus pane, slider at **0%** | Have a seat send a high-priority note: once with the app open, once with it in the background | **Silence** both times, with the notification still shown. Background log: `[FcmWake] muted by the TTS slider at 0%` | *(this commit)* |
| 11 | Focus → a message with an abstract | Look at the bubble, then tap the **Abstract** row | The bubble shows only the row (icon + "Abstract", or "Abstract · has a document"), none of the abstract's text. The tap opens the **whole** abstract as a document in the split. A doc link inside it replaces the abstract in the same half | *(this commit)* |

A failure on any line is a bug for Tiffany. Paste the line number and what you saw.
