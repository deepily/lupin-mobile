# v2 cutover — wave 2 readiness (eleven submit-shaped doors → `POST /api/v2/submit`)

**Author**: Tiffany 💍 · **Date**: 2026-08-21 · **Parent plan**: lupin `src/rnd/v0.2.0/2026.08.21-lupin-mobile-v2-cutover-plan.md` (María 🌸) · **Store row**: `a938907a` · **Integration HEAD read**: `d6d77f92` (Cheech: now `5e14c3db`)

## Verdict

`/api/v2/submit` exists on the integration branch (`wt-brain-integration-10ef4b64`, Rachel's step 10 `5050813d`) and the mobile side of the **door** is landed here (`SubmitRequest` + `QueueRepository.submit()` + 3 tests). **The eleven per-door swaps are held** for two server-side reasons (confirmed by Cheech 2026-08-21 18:30), neither of which the mobile repo can fix:

1. **No agentic job can run through `submit` yet.** `registry.py` builds the agentic `AgentSpec`s from `JOB_ARG_CONTRACTS` with `job_factory = None` ("phase 5 wires factory dispatch") and `AskFlow._run_agent` calls `spec.factory`. Rachel is adding the agentic-job path (`flow.py:234`) as the prerequisite for 11b; Cheech will DM the sha when it merges.
2. **The v2 arg vocabulary is the expeditor's, not the doors'.** `JOB_ARG_CONTRACTS[cmd].arg_mapping` knows `query / budget / audience / audience_context / research / source / task / dead_job_id / resume_from / test_types / pytest_args / dry_run / timeout / theme / target_duration_minutes / render_only …` — it has **no** `scheduled_at`, `monopolize`, `lead_model`, `worker_model`, `trust_mode`, `content_model`, `max_segments`, `target_languages`, `auto_fix_on_failure`, `env_vars`. Whether `submit` forwards unknown keys to the job constructor is undefined until the agentic path lands (question routed to Rachel via Cheech; she answers with the sha).

**Cheech's standing rule (from Pocholo's finding)**: only a handler that *pushes a job* is a door — `resume-from-checkpoint` stays v1 unless Rachel's handler read shows it pushes.

## Per-door mapping (mobile body key → v2 `args` key)

| # | mobile door | v2 `command` | required (contract) | maps cleanly | NOT in v2 vocabulary |
|---|---|---|---|---|---|
| 1 | `/api/deep-research/submit` | `agent router go to deep research` | `query` ✓ | budget, audience, audience_context | lead_model, dry_run, scheduled_at, monopolize |
| 2 | `/api/podcast-generator/submit` | `agent router go to podcast generator` | `research` — mobile sends **`research_source`** (rename) | — | target_languages (contract says `languages`), max_segments, dry_run, scheduled_at, monopolize |
| 3 | `/api/presentation-generator/submit` | `agent router go to presentation generator` | `source` — `source_path` is an accepted alias ✓ | target_duration_minutes, audience, theme, render_only | content_model, dry_run, scheduled_at, monopolize |
| 4 | `/api/swe-team/submit` | `agent router go to swe team` | `task` ✓ | budget, timeout, dry_run | trust_mode, lead_model, worker_model, scheduled_at, monopolize |
| 5 | `/api/bug-fix-expediter/submit` | `agent router go to bug fix expediter` | `dead_job_id` ✓ | extra_context, dry_run | scheduled_at, monopolize |
| 6 | `/api/test-suite/submit` | `agent router go to test suite` | (none) | test_types, pytest_args, dry_run | auto_fix_on_failure, env_vars, scheduled_at |
| 7 | `/api/test-fix-expediter/resume-from` | `agent router go to test fix expediter resume` | `resume_from` ✓ | — | — |
| 8 | `/api/deep-research-to-podcast/submit` | `agent router go to research to podcast` | `query` ✓ | budget, audience, audience_context | target_languages (→`languages`?), max_segments, dry_run |
| 9 | `/api/deep-research-to-presentation/submit` | `agent router go to research to presentation` | `query` ✓ | budget, target_duration_minutes, theme, audience, audience_context | lead_model, dry_run |
| 10 | `/api/push-agentic` | = `routing_command` (1:1) | per command | `args` + `question` carry over verbatim | scheduled_at, monopolize |
| 11 | `/api/jobs/{id}/resume-from-checkpoint` | **none** — prebuilt-`job` shape is in-process only | — | — | stays v1 per Cheech's rule unless the handler pushes a job |

`/api/deep-research/report` is a READ — not touched (plan ⚠️).

## What is landed now (this repo, uncommitted at time of writing)

- `SubmitRequest {command, args, question?, websocket_id?, speak}` (`queue_models.dart`) — no `interactive` flag: submit never parks.
- `QueueRepository.submit()` → `POST /api/v2/submit` → `AskResponse` (`queue_repository.dart`).
- Tests ×3 (`queue_repository_test.dart`): body shape + parse; null-omission + `needs_input` without `pending_id`; transport → `QueueApiException`. Full suite 585 ✅ / 0 ❌ outside quarantine.

## Rachel's answers (2026-08-21 18:42, measured on the integration branch)

1. **`scheduled_at` / `monopolize` are not constructor args anywhere.** Every v1 router sets them *after* building the job (`deep_research.py:172`, `podcast_generator.py:530/719`, `presentation_generator.py:230`, `bug_fix_expediter.py:107`, `mock_job.py:185`); `v2_ask.py` has no such step. Consequence: a `submit` body carrying them is silently dropped until the v2 handler grows the same post-construction step. **Open ask to Rachel**: will `/api/v2/submit` take `scheduled_at` / `monopolize` as top-level fields? Until answered, doors that need scheduling (1–6, 10) cannot flip without losing the feature. **RULED (Cheech 18:45)**: top-level `scheduled_at` / `monopolize` on `SubmitRequest`, serialized only when set; Rachel's successor lands the server side in 11b. **Mobile is already that shape**: `SubmitRequest.scheduledAt` / `.monopolize` (nullable, emitted only when set; unit test covers top-level placement + omission). Flip all nine doors on Cheech's integration sha.
2. **Both resume doors push a job** (`queues.py:1719` resume-from-checkpoint, `queues.py:1822` TFE resume-from) — so by Cheech's rule they *are* doors — but they build the job from a checkpoint (`resume_job(...)`), a shape `SubmitRequest {command, args, question}` cannot express. **Ruling: doors 7 and 11 stay v1.** Wave 2 is nine doors, not eleven.

**Ask-side caveat (Cheech 18:52)**: only `submit` can dispatch an agentic command (`c2176c71`); `ask` and `resume` cannot yet — bug `b7fe8941` on Rachel, right after 11b-1. The nine-door flip is submit-side and does not wait on it; anything mobile that *speaks* an agentic command through `/api/v2/ask` (wave-1 voice path) lands on the receptionist until it lands.

**Server sha (Rachel 19:05): `b12174fc`** — all three fields live on `SubmitRequest` and reach the job via the factory (`scheduled_at` ISO string · `monopolize` real bool, non-bool → 422 · `parent_id_hash` string → `job.spawned_by_id_hash`). In Pocholo's review; **flip held until his word comes through Cheech.**

**Mobile prep done (23:12Z, additive, no swaps)**: each door model carries `static const submitCommand` + `toSubmitArgs()` (renames: `research_source→research`, `target_languages→languages`, `source_path→source`; queue directives never in args), `PushAgenticRequest.toSubmitRequest()` for door 10; `test/unit/agentic/submit_mappers_test.dart` (6 tests, 60/60 across agentic+queue). Factory on `b12174fc` does not read `lead_model` for deep research nor `max_segments` for the podcast doors — carried anyway, harmless.

## ✅ FLIPPED (23:20Z, against integration `799e43d0` — Cheech's word 23:16Z)

Nine doors now POST `/api/v2/submit`: the eight `AgenticRepository.submitX()` go through one private `_submit( label, SubmitRequest )` (command = `X.submitCommand`, args = `req.toSubmitArgs()`, `question` = a human line, `websocket_id`/`scheduled_at`/`monopolize` top-level when the model carries them) and `QueueRepository.pushAgentic()` sends `req.toSubmitRequest()`. **Return types unchanged on purpose**: the bloc/UI contract stays `AgenticSubmitResponse` / `PushJobResponse`, built by `.fromAsk( AskResponse )` — `status == 'waiting'` + `job_id` is the success; `needs_input` (→ "Missing: …"), receptionist, `failed`, or any no-job body throws `AgenticApiException` / `QueueApiException` so the form shows a Failure with the server's words rather than a blank success. `queue_position` is 0 (v2 reports none). Door 7 (TFE resume-from) and 11 stay v1. Tests: repo mocks moved to `POST /api/v2/submit` with v2 bodies, +3 repo semantics tests, +2 pushAgentic tests — agentic+queue 85/85.

## Flip procedure (when Rachel's path lands) — executed above, kept for the record

For each door: keep the door's Dart request model as the UI's input; add `toSubmitArgs()` producing the v2 keys (renames in the table); `agentic_repository.submitX()` → `queue.submit( SubmitRequest( command: …, args: req.toSubmitArgs(), question: <human line> ) )`; return type `AskResponse` (the synchronous §8 shape — **not** `AgenticSubmitResponse {status, job_id, queue_position}`; a long job comes back `status == 'waiting'` + `job_id`, which is a SUCCESS); tests move the mock key **and** the fixture body together. The ten unmapped keys need the server ruling first: forward-as-kwargs, drop, or new contract entries.
