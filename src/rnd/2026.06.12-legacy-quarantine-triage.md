# Legacy Quarantine Triage — Per-File Disposition

**Date**: 2026.06.12
**Author**: Mr. Radio 🦉 (session `dabf7fbb`), inline-authored (the spawned worker died on the
weekly usage limit).
**Scope**: every file in `test/legacy_quarantine/` (25 files = 21 tests + 4 `.mocks.dart`),
quarantined 2026-04-16 during Tier 1+2 migration. Closes the standing TODO debt (filed
2026-05-11): map each to current coverage + disposition. Read-only audit — NOTHING deleted here.
**Cross-ref**: `2026.06.12-legacy-voice-stack-retirement-scoping.md` already dispositions 7 of
these (drop-with-retirement); this doc does not re-litigate those — it marks them and dispositions
the remaining 18.

> **Convention (binding)**: quarantine-first — drift-broken tests + their `.mocks.dart` are
> DELETED only after new coverage proves the legacy signal redundant (auto-memory
> `feedback_legacy_test_quarantine`). Every disposition below respects that gate.

---

## Disposition summary

| Disposition | Count | Meaning |
|---|---|---|
| **RIDES-RETIREMENT** | 11 | Subject is in the current Tier A+B legacy-voice-stack retirement's delete set; the test drops WITH it (no independent action). |
| **RIDES-FOLLOW-ON** | 11 | Subject is the *enhanced-WS / adaptive / cache* legacy cluster the retirement doc flagged as a SEPARATE follow-on debt; keep quarantined until that retirement is scoped. |
| **RESURRECT-CANDIDATE** | 2 | Subject is alive AND may be on a live code path (analytics dashboard) with no active test — the only real lost-coverage flag; needs a liveness check before delete. |
| **mock (rides owner)** | (4, counted within the above) | `.mocks.dart` files drop with their owning test, never independently. |

**Net**: zero files recommended for immediate independent delete. Everything either rides a
retirement (current or follow-on) or needs a liveness verdict first — consistent with
quarantine-first.

## Per-file table

| File | Surface under test | Surface status (lib) | Active coverage now | Disposition |
|---|---|---|---|---|
| `unit/voice_bloc_test.dart` | `VoiceBloc` | alive, Tier B orphan | focus_mode bloc suites (different bloc) | **RIDES-RETIREMENT** |
| `repositories/voice_repository_test.dart` | `VoiceRepository` | alive, Tier B orphan | — | **RIDES-RETIREMENT** |
| `repositories/audio_repository_test.dart` | `AudioRepository` impl | alive, Tier B orphan | — | **RIDES-RETIREMENT** |
| `services/audio/audio_cache_manager_test.dart` | `AudioCacheManager` | alive, **0 lib importers** | — | **RIDES-RETIREMENT** |
| `services/audio/audio_cache_manager_test.mocks.dart` | (mocks) | — | — | **mock → rides owner** |
| `di/dependency_injection_test.dart` | legacy DI (`tts_service`, `repository_provider`) | Tier B orphan | `test/service_integration/focus_app_wiring_test.dart` + unit DI | **RIDES-RETIREMENT** |
| `integration/service_integration_test.dart` | legacy services wiring | Tier B orphan | focus wiring + unit suites | **RIDES-RETIREMENT** |
| `core/cache/voice_recording_cache_test.dart` | `VoiceRecordingCache` | alive, imported only by `audio_cache_manager`+`eviction_manager` (Tier B) | — | **RIDES-RETIREMENT** |
| `core/cache/voice_recording_cache_test.mocks.dart` | (mocks) | — | — | **mock → rides owner** |
| `repositories/job_repository_test.dart` | `JobRepository` | alive, use-case-registry cluster (Tier B) | — | **RIDES-RETIREMENT** |
| `repositories/session_repository_test.dart` | `SessionRepository` | alive, use-case-registry cluster (Tier B) | — | **RIDES-RETIREMENT** |
| `repositories/user_repository_test.dart` | `UserRepository` | alive, use-case-registry cluster (Tier B) | — | **RIDES-RETIREMENT** |
| `repositories/storage_integration_test.dart` | repo storage integration | Tier B orphan cluster | — | **RIDES-RETIREMENT** |
| `cache/cache_system_test.dart` | `cacheAudioForText` (legacy audio cache) | Tier B cache cluster | — | **RIDES-FOLLOW-ON** (cache legacy) |
| `integration/cache_integration_test.dart` | audio-cache integration (`audio.jobId`) | Tier B cache cluster | — | **RIDES-FOLLOW-ON** |
| `services/websocket/enhanced_websocket_service_test.dart` | `EnhancedWebSocketService` | alive, **enhanced-WS cluster** (6 importers, all enhanced-WS) | active WS is `websocket_service.dart` (S5-touched) | **RIDES-FOLLOW-ON** (enhanced-WS debt) |
| `services/websocket/enhanced_websocket_service_test.mocks.dart` | (mocks) | — | — | **mock → rides owner** |
| `services/websocket/websocket_message_router_test.dart` | `WebSocketMessageRouter` | alive, enhanced-WS cluster | — | **RIDES-FOLLOW-ON** |
| `connection/connection_recovery_test.dart` | WS reconnect (live-server, hangs 21m) | enhanced-WS cluster | S5 reconnect-upsert unit tests cover the ACTIVE path | **RIDES-FOLLOW-ON** |
| `integration/event_system_integration_test.dart` | WS event system (live-server) | enhanced-WS cluster | — | **RIDES-FOLLOW-ON** |
| `integration/websocket_integration_test.dart` | WS integration (live-server) | enhanced-WS cluster | — | **RIDES-FOLLOW-ON** |
| `integration/failure_recovery_test.dart` | failure/recovery (compile error) | enhanced-WS cluster | — | **RIDES-FOLLOW-ON** |
| `performance/websocket_performance_test.dart` | WS perf (hangs on network) | enhanced-WS cluster | — | **RIDES-FOLLOW-ON** |
| `core/monitoring/performance_monitor_test.dart` | `PerformanceMonitor` | alive, imported by `analytics_dashboard`+`dashboard_models` | **NONE found** | **RESURRECT-CANDIDATE** ⚠ |
| `core/monitoring/performance_monitor_test.mocks.dart` | (mocks) | — | — | **mock → rides owner (resurrect)** |

## Lost-coverage section (class RESURRECT-CANDIDATE)

**`PerformanceMonitor`** (`lib/core/monitoring/performance_monitor.dart`) — the ONE surface that
is (a) alive, (b) imported by something that is NOT obviously Tier-B orphan
(`analytics_dashboard.dart` + `dashboard_models.dart`), and (c) has **no active test coverage**
found in the current suites. This is the only genuine lost-coverage flag in the quarantine.

- **Before any delete**: verify whether `analytics_dashboard` is a LIVE feature (routed/rendered)
  or itself orphaned. If LIVE → write a minimal `PerformanceMonitor` unit test (the quarantined
  one drifted on `PerformanceMonitorConfig` constructor — a thin re-author against the current
  constructor closes the gap) and KEEP the surface. If ORPHANED → it joins a Tier-B-style
  retirement and the test rides it.
- Recommended minimal test: construct `PerformanceMonitor` with the current
  `PerformanceMonitorConfig`, exercise one record/report cycle, assert the emitted metric shape.

## Proposed execution order

1. **Now (on Rick's word)**: nothing deletes independently — the whole quarantine is gated.
2. **With the current Tier A+B retirement** (TODO item 5, post-PoC): drop the 11 RIDES-RETIREMENT
   files (incl. their mocks) as part of that session's P3 quarantine-drop step; the retirement doc
   already lists 7, this adds the use-case-registry repos + voice_recording_cache as the same
   cluster — confirm the expanded drop list at retirement time.
3. **Scope a follow-on retirement** for the enhanced-WS / adaptive / cache legacy cluster (11
   RIDES-FOLLOW-ON files ride it); the retirement doc already named this as separate debt.
4. **Resolve the `PerformanceMonitor` flag first** (liveness check on analytics_dashboard) — it's
   the only file whose disposition could be "keep + add coverage" rather than "drop".

## Baseline reconciliation

The README documents 25 quarantined files; this triage accounts for all 25 (21 tests + 4 mocks).
The historical "44 ❌" baseline counts *individual failing test cases*, not files — the 21 test
files expand to ~44 cases. The file-level inventory fully reconciles; the case-level 44 is a
property of these same 21 files and needs no separate disposition (the files carry the cases).
