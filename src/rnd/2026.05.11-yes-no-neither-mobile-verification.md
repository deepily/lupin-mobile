# Yes/No/Neither Mobile Verification — 2026-05-11

**Session**: `51ee0afa` (continues from `fe7679c9` TODO triage)
**Question asked**: Has the Lupin Mobile app implemented the updated cosa-voice yes/no/**neither** contract for `ask_yes_no`?

## Verdict

**❌ NOT implemented.** The mobile prompt sheet still ships the 2-button binary (Yes / No only). The third `"neither"` response value defined by the current cosa-voice MCP contract has no corresponding UI button, test key, or emit path in the mobile codebase.

## cosa-voice contract (loaded this session, MCP v0.3.0)

`ask_yes_no` returns one of:
- `"yes"` / `"no"` / `"neither"`
- optionally suffixed `"yes [comment: ...]"`, `"no [comment: ...]"`, `"neither [comment: ...]"`

`neither` is **never a default** — it requires an explicit user click and signals "the question needs re-framing," not a soft yes/no.

## Evidence

| Surface | File | Current state | Gap |
|---|---|---|---|
| Prompt sheet UI | `lib/features/notifications/presentation/interactive_prompt_sheet.dart:119-155` (`_YesNoBody`) | Row with 2 buttons: `OutlinedButton "No"` + `FilledButton "Yes"` | No third "Neither" button |
| Submit payloads | same file, lines 139 + 147 | Hardcoded literals `"no"` and `"yes"`, both wrapped by `_withComment(...)` | No path emits `"neither"` |
| Test keys | `lib/core/testing/test_keys.dart:23-24` | `promptYesButton = 'prompt.yes'`, `promptNoButton = 'prompt.no'` | No `promptNeitherButton` |
| Bloc event | `lib/features/notifications/domain/notification_event.dart:56-66` (`NotificationsRespond`) | Carries `dynamic responseValue` (permissive) | No structural blocker, but no caller emits "neither" |
| Whole-tree probe | `grep -rn "neither" lib/ test/ integration_test/` | **Zero hits** | Feature work has not begun |

## Status alignment

This matches the priority-1 NEXT-SESSION TODO entry recorded at `TODO.md:7-9` in the prior planning-only session `fe7679c9` (commit `29e8d6d`, 2026-05-11). Implementation is still queued, not started.

## Scope to land the feature (per existing TODO note + this scan)

1. **`_YesNoBody`** — add a third button (recommend `TextButton` to visually de-emphasize since `neither` is never a default), emitting `_withComment("neither")`.
2. **`TestKeys`** — add `promptNeitherButton = 'prompt.neither'`.
3. **`NotificationsRespond`/repository** — already permissive (`dynamic responseValue`); no plumbing change unless parent-Lupin POST contract has its own enum gate.
4. **Tests** — extend `conversation_screen_test.dart` and any `interactive_prompt_sheet_test.dart` with a third-button case + emit assertion.
5. **Open backend-contract question** — confirm parent-Lupin's response endpoint accepts `"neither"` literal, or whether server-side extension is needed first.

## Out of scope of this verification

- Implementation itself (deferred to a fresh planning session; user-confirmed via prior session's TODO entry).
- Parent-Lupin contract probe (G-gate-style propagation check; do at start of implementation session).
