# Focus-Mode Voice Chat — Status Summary

**Date**: 2026-06-23
**Author**: Tiffany 💍 (session `e02f3101`)
**Branch**: `wip-v0.1.6-2026.04.16-tracking-lupin-work`
**Plan-of-record**: `src/rnd/2026.06.11-focus-mode-voice-chat/` (see `00-index.md`)
**Purpose**: One-page readable answer to "where are we on the focus UI?" — accomplished vs. remaining.

---

## Headline

The **focus-mode voice-chat + FCM wake-up milestone is AI-complete, reviewed, and committed.**
All six sections (S1–S6) are built and green. Everything that is left is **HUMAN / on-device**
work — nothing is AI-blockable. The whole batch is committed locally (`b34fa01`) but **not pushed**
(push stays Rick-gated).

---

## ✅ What's accomplished

### Planning + review
- **12-doc plan-set authored** under `src/rnd/2026.06.11-focus-mode-voice-chat/`.
- Run through the full 3-stage `/plan-review-cascaded` pipeline — **cascade CLOSED 2026-06-12T03:09Z**.
  - ~43 findings processed; 0 foundational survived; every re-litigation round closed in one pass.
  - 2 user rulings (F-S1-1 `enqueueAlways`; F-S5-S2-1 FCM handler-does-the-work).
  - All 7 open sub-questions (OSQs) ratified into `02-decisions.md`.

### Implementation (all 6 sections)

| Section | What it delivers | Status |
|---|---|---|
| **S1** | TTS pause/resume (TtsOrchestrator extension) | ✅ implemented + light-review (1 finding fixed+pinned) |
| **S2** | FocusChatBloc (state + session registry) | ✅ implemented + light-review (2 findings fixed+pinned) |
| **S3** | Focus UI surface (badge rail + chat pane + route swap) | ✅ implemented + light-review (zero findings) |
| **S4** | Voice input → Whisper ASR → reply | ✅ implemented + light-review (1 cosmetic fixed) |
| **S5** | FCM silent-relay handler (mobile) | ✅ AI-implemented — §3.2.4 milestone awaits on-device probe |
| **S6** | FCM backend interface (parent Lupin) | ✅ merged parent-side (`83990552`), AC-S6.1 6/6 on :8000 |

### Quality + delivery state
- **Mobile test suite: 406 ✅ / 1 by-design skip / 0 ❌.**
- **Committed `b34fa01`** — 55 files, +7,046 / −441; `io/` session scratch excluded; **NOT pushed**.
- **Postgame walkthrough complete** — 6 of 7 agenda items decided (the two-file-contract item
  parked pending Rick's read of the explainer). Decisions recorded in `TODO.md §POSTGAME DECISIONS`.

---

## ⏳ What remains (all HUMAN / on-device gated)

These need real hardware — FCM, doze, mic, ear — so they cannot be automated away:

1. **OSQ-7 Firebase console pass** (~10 min, HUMAN) — runbook
   `src/rnd/2026.06.11-focus-mode-voice-chat/91-osq7-firebase-console-runbook.md`.
   Provisions the Firebase project/app + service-account key. Then AI runs the validation checklist.
2. **Laptop build wiring + S5 Phase-0 FCM probe** (HUMAN-executed, AI-scripted) — runbook
   `92-s5-phase0-fcm-probe-runbook.md`. This is the step that **declares** the §3.2.4 FCM milestone
   (or triggers the shape-3 fallback).
3. **On-device acceptance gates** — S5 HUMAN doze gate + Stage-1 "focus-mode milestone" fm1–fm6.
4. **Push the committed batch** — waiting on Rick's explicit word.
5. **Full legacy-voice-stack retirement** (incl. `flutter_sound`) — scoped GO exists
   (`src/rnd/2026.06.12-legacy-voice-stack-retirement-scoping.md`), but Rick's ruling is
   **WAIT until we have a working PoC together**.

---

## 🚀 Fastest path to seeing it on a real device

There is a **working-PoC fast path that needs NO Firebase**:
`src/rnd/2026.06.12-poc-laptop-build-runbook.md`

- rsync → `flutter build` → `adb install`.
- Real-device gotcha: repoint `assets/config/server-contexts.json` off the emulator-only
  `10.0.2.2` to the dev-server LAN IP.
- Ends at the fm1–fm6 acceptance, with FCM OFF.

This gets the focus UI running on Rick's device the fastest, decoupled from all the Firebase work.

---

## 📌 Context: how this ranks against Rick's stated priorities

Per `TODO.md`, the cleared-session priorities to Monday were **(1) task-list functionality** and
**(2) cosa-voice token efficiency** (~75% of inference budget). **GCP/FCM migration is
BACK-BURNERED.** So the focus-UI device work above is real and ready, but sits **below** those two
on Rick's own list — the PoC fast path is the cheapest way to "close the loop" on focus-mode
without reopening the back-burnered Firebase track.

---

## Source documents

- Master index: `src/rnd/2026.06.11-focus-mode-voice-chat/00-index.md`
- Decisions log: `src/rnd/2026.06.11-focus-mode-voice-chat/02-decisions.md`
- Postgame: `src/rnd/2026.06.12-joint-postgame-focus-mode-build.md`
- PoC fast path: `src/rnd/2026.06.12-poc-laptop-build-runbook.md`
- Live TODO: `TODO.md` (§NEXT SESSION — focus-mode HUMAN device items)

---

## Session progress addendum — 2026-06-23 (Tiffany 💍, session e02f3101)

Events after the summary above, in order:

1. **FCM live-service track reactivated** (Rick directive) — un-back-burnered. Coordinated end-to-end
   with Tiberius 👑 (thread). Division of labor: Tiffany = client, Tiberius = server.
2. **Auth model = keyless ADC** (no service-account JSON key needed after all; VM SA granted
   `roles/firebasecloudmessaging.admin`).
3. **Project-alignment catch (resolved):** `android/app/google-services.json` `project_id` =
   `hello-world-foo-423219` (an existing reused project, not a dedicated `lupin-mobile` one).
   Tiberius file-verified device-registration project == server-send project == IAM grant → no
   sender-mismatch. **NON-BLOCKING decision parked for Rick:** reuse vs. clean dedicated project.
4. **Client wiring done:** google-services.json present + Google Services Gradle plugin pre-wired
   (`settings.gradle.kts` 4.4.2 + `app/build.gradle.kts`) → runbook-92 laptop pass collapses to the
   on-device probe only.
5. **APK BUILD FIX (record plugin skew):** Rick's `flutter build apk` failed compiling Dart-only
   `record_linux 0.7.2` (didn't implement `record_platform_interface` 1.6.0's
   `hasPermission(request:)`/`startStream`). Fix: `pubspec.yaml` `record ^5.1.2 → ^6.0.0` (resolves
   6.2.1, bumps `record_linux → 1.3.1`). `record_windows 1.0.7` stale = non-issue (native plugin, no
   Dart compile, no Windows target). record 7 ruled out (needs Dart 3.12; toolchain is 3.9).
   **Verified on dev server:** pub get clean · `flutter analyze` no-issues on record-using files ·
   15/15 ASR + voice-reply tests pass (incl. live `:7999` WAV→transcript). Android-SDK kernel build
   itself unverifiable here (no SDK) — laptop rebuild pending. Note: `pubspec.lock` is gitignored, so
   the fix propagates via `pubspec.yaml` on re-resolve.
6. **FCM SERVER LIVE** (Tiberius live-service-up): image `lupin:1.2.0`, `firebase_admin 6.9.0`,
   FcmWakeService ENABLED via keyless ADC; ADC→FCM send-auth proven. Server task closed his end.

**Current state — ONE remaining step, human/device-gated:** Rick rsyncs `pubspec.yaml` → rebuilds APK
(now compiles) → installs on emulator/device → we run the runbook-92 wake probe together (register
token → wake with WS closed → confirm push) → receipt to Tiberius → §3.2.4 milestone declared (or
shape-3 fallback). Store task `63790ce3` blocked on this device probe; hold file current.

**Checkpoint status:** NOT yet committed (Rick drives commit cadence). Uncommitted in Tiffany's scope:
`pubspec.yaml`, `android/{settings,app/build}.gradle.kts`, `TODO.md`, `README.md`, this doc.
Excluded from any Tiffany commit: `android/app/google-services.json` (Rick/Tiberius-placed + open
project-reuse decision), `io/` (scratch), `.heartbeat-hold-*.json` (runtime state).
